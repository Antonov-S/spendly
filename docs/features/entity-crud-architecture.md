# Entity CRUD Architecture → moved

**This file is not maintained.** The canonical, single source of truth for the entity read/mutation
contracts now lives at:

➡️ [`../entity-crud-architecture.md`](../entity-crud-architecture.md) (i.e. `docs/entity-crud-architecture.md`)

This stub used to hold a full copy that drifted out of sync with the top-level version (stale
account route `/settings` vs the shipped `/accounts`, wrong fetcher filename, missing
archived-account rules). To prevent that class of bug, there is now exactly one copy. Edit
`docs/entity-crud-architecture.md` only; do not restore content here.

Feature specs in this folder should link the reference docs as `../entity-crud-architecture.md` and
`../entity-types.md`.
