# Current Feature: JWT Session Revocation

## Status

In Progress

## Goals

- Add a monotonic `User.sessionEpoch` revocation signal for JWT sessions.
- Stamp the epoch into JWTs at sign-in and reject tokens when the server-side epoch changes, the user is soft-deleted, or the user row is gone.
- Bump the epoch on password change, password reset, and account soft-delete.
- Make signed-in checks consistently require `session.user.id` to avoid redirect loops.
- Explicitly sign the current user out after password change and show a sign-in success banner.
- Cover the revocation helper, epoch-bumping mutations, guard predicates, and password-change sign-out behavior with Vitest tests.

## Notes

- Spec: `docs/fixes/jwt-session-revocation-spec.md`.
- Current sessions use `session: { strategy: "jwt" }` in `src/auth.ts`; no server-side revocation exists today.
- Additive Prisma migration required: `add_user_session_epoch`, applied only through `prisma migrate dev` on the development Neon branch.
- New helper planned at `src/lib/auth/session-epoch.ts`: missing token epoch counts as `0`, missing/deleted user row revokes, and any epoch mismatch revokes.
- `jwt` callback belongs in `src/auth.ts` (Node runtime), not `auth.config.ts`; `auth.config.ts` remains edge-safe.
- Password change should call `signOut({ redirectTo: "/sign-in?passwordChanged=1" })` after successful hash rotation instead of returning an in-place success state.
- Existing tests cover auth utilities/actions; reset-password route coverage appears absent and should be added for the epoch bump.
- Lint gate required registering the already-configured `react-hooks` flat-config plugin, adding the missing dev dependency, and ignoring `.codex/**` tooling/cache files so app lint does not scan agent assets.
- No open product questions found during load; implementation should follow the spec as written.

## History

- **Initial Next.js and Tailwind setup** — Bootstrapped the project with Next.js 16 / React 19, TypeScript, and Tailwind CSS v4. Moved app into `src/`, removed Create Next App boilerplate, added `.gitignore` rules for AI tooling files (`CLAUDE.md`, `AGENTS.md`, `context/`, `.claude/`) and env files (committing `.env.example` only), and pushed to origin.

- **Enable React Compiler** — Added `reactCompiler: true` to `next.config.ts`.

- **Prisma + Neon PostgreSQL Setup** — Set up Prisma 7 with the new `prisma-client` generator (output to `src/generated/prisma`, gitignored) wired to Neon via the `@prisma/adapter-pg` driver adapter. Added the full data model from `project-overview.md` (financial accounts, transactions, budgets, recurring templates/drafts, goals/contributions, NextAuth models, enums, indexes, cascades). Configured `prisma.config.ts` to use the unpooled `DIRECT_URL` for migrations while the runtime singleton (`src/lib/prisma.ts`) uses the pooled `DATABASE_URL`. Created and applied the `init` migration, seeded 20 system categories, and added `db:*` scripts plus `scripts/test-db.ts` for connectivity checks. Incidental fixes: corrected the `tsconfig` `@/*` alias to `./src/*` and added the missing `babel-plugin-react-compiler` dependency so the build passes.

- **Dashboard UI Mockup** — Built a visual-only prototype of `/dashboard` (no DB/fetching; clearly-flagged mock data in `src/lib/mock-data.ts`). Layout shell: collapsible sidebar (full ≥1024px / icon-only 768–1024px / mobile hamburger overlay) with bottom-pinned Help + user profile row; topbar with a custom wallet `Logo` and right-aligned account selector pill (search and bell deliberately omitted per spec; wordmark hidden on small screens). Main area: page header (hero balance + dependency-free SVG `Sparkline`, period pills, prominent Add CTA), enlarged monthly metric strip (Income/Expenses/Cashflow), and `[1.4fr_1fr]` content columns — date-grouped transactions table (Today/Yesterday/Earlier + category-color accent) on the left, Budgets panel (semantic green/amber/red progress) + Goals widget on the right. Slide-in transaction drawer (420px right panel desktop / ~90vh bottom sheet mobile, 300ms ease + backdrop) and mobile bottom nav with floating add button. Added Tailwind v4 theme tokens (dark default) in `globals.css`, pure utilities (`format.ts`, `budget.ts`), shared types (`types/dashboard.ts`), and split constants (types/enums in `system-constants.ts`, UI data in `constants.ts`). All component files kebab-cased. An interim "needs attention" insights strip was added then removed at user request. Build passes; Vitest not yet set up, so `format.ts`/`budget.ts` remain untested (deferred to `/feature test`).

- **Seed Script** — Overwrote `prisma/seed.ts` with idempotent demo data. Keeps the 20 system categories (unchanged). Adds two demo users (`demo-nonpro@spendly.io` / `demo-pro@spendly.io`, passwords hashed with `bcryptjs` 12 rounds). Demo Pro gets 4 financial accounts (Checking, Savings, Cash, Credit Card), ~37 transactions across April–June 2026, 7 June budgets, and 3 goals (Japan Trip, Emergency Fund, New Laptop) with contribution records. Demo Free gets 2 accounts, 5 June transactions, 3 budgets, and 1 goal. Amounts are signed (negative = expense) to match the `startingBalance + SUM(amounts)` balance formula. Re-running the seed deletes and recreates demo users cleanly via cascade. Installed `bcryptjs` + `@types/bcryptjs`.

- **Test Setup** — Installed Vitest 4 (only test dependency; no jest/jsdom/testing-library). Created `vitest.config.ts` (node environment, `fileParallelism: false` + `isolate: true` as next-auth/next/server workaround, `tsconfigPaths: true`) and `vitest.setup.ts` (stubs `server-only`). Added `test` (watch) and `test:run` (CI) scripts to `package.json`. Created `test/` directory mirroring `src/`. Wrote 28 passing tests: `test/lib/format.test.ts` covers `formatCurrency` and `formatSigned`; `test/lib/budget.test.ts` covers `budgetFraction`, `budgetState`, `budgetPercent`, and `budgetColor`. All tests pass and build passes.

- **Dashboard Data** — Replaced all mock data on the dashboard with live Neon/Prisma queries. Created `src/lib/db/dashboard.ts` with five server-only fetchers (`getDashboardSummary`, `getBalanceTrend`, `getRecentTransactions`, `getBudgetsData`, `getGoals`) that return the existing `src/types/dashboard.ts` types. Created `src/lib/icon-map.ts` to resolve DB icon-name strings to Lucide components. Dashboard page is now `async`, marked `force-dynamic`, and fans out all fetches in a single `Promise.all`. All panel components (`TransactionsPanel`, `BudgetsPanel`, `GoalsWidget`, `PageHeader`) converted from mock-data imports to typed props. Transaction left-border color now derives from `TransactionType` (green INCOME / red EXPENSE / grey TRANSFER) via a `TYPE_BORDER_COLOR` lookup. Budget remaining label is dynamic from `new Date()`. Goal colors cycle through a five-color palette (no color field in schema). `devIndicators: false` added to `next.config.ts`. Auth placeholder: demo-pro user fetched by email until NextAuth session is wired up. Build and all 28 tests pass.

- **Auth Setup - NextAuth + Google Provider** — Installed `next-auth@beta` and `@auth/prisma-adapter`. Set up the split config pattern: `auth.config.ts` holds the edge-compatible Google provider config and the `authorized` callback that gates `/dashboard/*` to signed-in users; `auth.ts` combines it with the Prisma adapter and JWT session strategy, adding a `session` callback to expose `user.id`. Created `src/app/api/auth/[...nextauth]/route.ts` for the NextAuth GET/POST handlers, `src/proxy.ts` (Next.js 16 proxy re-export) with a broad matcher, and `src/types/next-auth.d.ts` extending `Session` with `user.id`. Added `AUTH_GOOGLE_ID` and `AUTH_GOOGLE_SECRET` to `.env.example`. Build and all 28 tests pass.

- **Auth Credentials - Email/Password Provider** — Added Credentials provider to the NextAuth split config (`auth.config.ts` placeholder, `auth.ts` real bcrypt validation via `verifyCredentials`). Created `POST /api/auth/register` with Zod validation (name/email/password/confirmPassword, passwords-match refine, email normalized to lowercase), bcrypt hashing, duplicate-email check, returning `{success, user}` on 201 or `{error}` on 400/409. Installed `zod`; schemas in `src/lib/validations/auth.ts`; core logic in `src/lib/auth/register.ts` and `src/lib/auth/credentials.ts` (both `server-only`). Added `BCRYPT_SALT_ROUNDS = 12` and `PASSWORD_MIN_LENGTH = 8` to `system-constants.ts`. Dashboard page replaced the `getDemoUserId()` placeholder with `auth()` session; sidebar now renders the real user's name, email, and computed initials (or avatar image for OAuth users). 13 new Vitest tests for `registerUser` and `verifyCredentials` (41 total, all pass). Build passes.

- **Auth UI - Sign In, Register & Sign Out** — Replaced NextAuth default pages with custom UI. Custom `/sign-in` (email/password + Google OAuth, link to register) and `/register` (full Zod client-side validation, Google OAuth, redirects to sign-in on success) pages. Sidebar bottom profile row converted to a `UserMenu` drop-up with Profile link and sign-out action. New `/profile` page shows real user data (avatar, name, email, plan, currency, member since) with sign-out. Reusable `Avatar` component (Google image or initials fallback) and `getInitials` util extracted to `src/lib/avatar.ts`. Auth guard helpers extracted to `src/lib/auth/guards.ts` (`getSessionOrRedirect` / `redirectIfAuthenticated`) replacing duplicated inline auth checks across four pages. `auth.config.ts` updated with `pages.signIn` and redirect logic for protected/public routes. `InputFormField`, `SubmitButton`, `GoogleButton`, `AuthCard` shared auth components added in `src/components/auth/`. 19 new Vitest tests (60 total); build passes.

- **Email Verification (Resend)** — Installed `resend`. After email/password registration, a 32-byte single-use token (24h TTL) is created via the existing `VerificationToken` model and emailed via Resend (`onboarding@resend.dev`). `GET /api/auth/verify-email?token=<raw>` consumes the token, sets `User.emailVerified`, and redirects to `/sign-in?verified=1`. `POST /api/auth/resend-verification` re-issues a link (always returns generic success to avoid enumeration). Credentials sign-in blocks unverified users (isolated check after password validation, ready for a future dev toggle). `/verify-email` page shows "Check your inbox" with a resend form prefilled from `?email=`. `register-form` now redirects to `/verify-email` on success; sign-in page shows a `?verified=1` success banner and a permanent "Resend the link" path. Email send failure during registration is caught and logged without failing the response. `VERIFICATION_TOKEN_TTL_HOURS` and `EMAIL_FROM` added to `system-constants.ts`; `AUTH_URL` documented in `.env.example`. 9 new Vitest tests (69 total); build passes.

- **Email Verification Flag** — Added `EMAIL_VERIFICATION_ENABLED` env var (read as `process.env.EMAIL_VERIFICATION_ENABLED !== "false"`) to `system-constants.ts`. When `false`: `register.ts` skips token creation and the Resend call; `credentials.ts` skips the `emailVerified` null-check; the resend-verification endpoint skips issuing new tokens; and `register-form.tsx` redirects to `/sign-in` instead of `/verify-email`. The redirect decision is passed as an `emailVerificationEnabled` prop from the server component (`register/page.tsx`) to the client form. Defaults to on — production needs no extra config. `.env.example` updated; local `.env` set to `false` for dev. 2 new Vitest tests covering both flag states for `registerUser` and `verifyCredentials` via a hoisted `vi.mock` toggle (71 total); build passes.

- **Fix: Contextual Resend-Verification Link on Sign-In** — The "Haven't verified your email? Resend the link" prompt was rendered unconditionally on the sign-in page, violating "never UI without backing function" when `EMAIL_VERIFICATION_ENABLED=false`. `sign-in/page.tsx` now reads `EMAIL_VERIFICATION_ENABLED` and passes it as `emailVerificationEnabled` to `<SignInForm>`. The form guards the resend-link block on `emailVerificationEnabled && state.error`, so it only appears when verification is on and a sign-in attempt has failed. No backend changes, no new tests. 71 tests and build pass.

- **Forgot Password / Password Reset** — Reuses `VerificationToken` model with a `reset:` identifier prefix to namespace reset tokens away from email-verification tokens (no schema changes). `createPasswordResetToken` clears only `reset:`-prefixed rows; `consumePasswordResetToken` rejects bare-email verification tokens without deleting them. `POST /api/auth/forgot-password` always returns generic `{ success: true }` (no enumeration); email is only sent when the user has a non-null `password`. `POST /api/auth/reset-password` consumes the token, bcrypt-hashes the new password, and sets `emailVerified`. Token TTL is 1 hour (`PASSWORD_RESET_TOKEN_TTL_HOURS`). Added `/forgot-password` and `/reset-password` pages with `ForgotPasswordForm` / `ResetPasswordForm` client forms (mirror existing `AuthCard`/`InputFormField`/`SubmitButton` patterns). `sign-in-form.tsx` now shows a "Forgot password?" link (always visible, not gated by verification flag). `forgotPasswordSchema` and `resetPasswordSchema` added to `src/lib/validations/auth.ts`. 8 new Vitest tests (79 total); build passes.

- **Profile Page** — Extended `/profile` with usage stats, change-password, and delete-account. Added `User.deletedAt` (migration `add_user_deleted_at`) for 30-day grace-period soft-delete — sign-in is blocked immediately for both credentials and Google OAuth once `deletedAt` is set. `getProfileStats` fetches per-item-type counts (accounts, transactions, budgets, goals, recurring). `ProfileStats` server component renders them in a grid. `ChangePasswordForm` (client) uses `useActionState` + re-auth via `changeUserPassword`; rendered only when `user.password !== null`. `DeleteAccountDialog` uses native `<dialog>` API — requires typing the account email to confirm; on success signs out and redirects to `/sign-in?deleted=1`. `scripts/reactivate-account.ts` added as a dev utility to clear `deletedAt`. 11 new Vitest tests (90 total); build passes.

- **Fix: OAuthAccountNotLinked Friendly Messaging** — When NextAuth redirects to `/sign-in?error=OAuthAccountNotLinked` (credentials user attempting Google OAuth with the same email), the sign-in page now shows an amber informational banner: "This email is already registered with a password. Please sign in with your email and password instead." `sign-in/page.tsx` adds `error?: string` to `searchParams`, narrows it to the literal `"OAuthAccountNotLinked"`, and passes it as `oauthError` prop. `sign-in-form.tsx` renders the banner at the top alongside other status banners. No new tests, no new routes, no new constants. 90 tests and build pass.

- **Fix: Profile Page UI Inconsistencies** — Four targeted Tailwind-only fixes across three files. Added `w-full` to the "Delete account" trigger button (`delete-account-dialog.tsx`) so it matches every other button on the page. Replaced the plain "Pro"/"Free" text with a `PlanBadge` pill component (`profile/page.tsx`): Pro = `text-success border-success/30 bg-success/10`, Free = neutral `text-ink-2 border-line`; `ProfileRow.value` widened from `string` to `ReactNode`. Added an "Account" section heading above the detail rows card to match the visual rhythm of "Your data", "Change password", and "Delete account" cards. Zero-value stats in `profile-stats.tsx` now render in `text-ink-2` instead of `text-ink` to de-emphasize empty counts. No logic changes, no new tests. 90 tests and build pass.

- **Fix: Hash verification & password-reset tokens at rest** — Raw 256-bit tokens are no longer stored in the `VerificationToken` table. New `src/lib/auth/token-hash.ts` exposes `hashToken(raw)` (SHA-256 hex, server-only). Both `createVerificationToken` (`verification.ts`) and `createPasswordResetToken` (`password-reset.ts`) now store `hashToken(rawToken)` and return the raw value for the link; their consume counterparts hash the incoming URL token before `findUnique`/`delete`. No schema change (64-char hex fits the existing `String @unique` column), no call-site changes (route handlers and `register.ts` see the same create/consume contract). Outstanding raw-stored tokens silently invalidate on deploy — acceptable given 24h/1h TTLs. 6 new Vitest tests (96 total): `token-hash.test.ts` suite + hashed-lookup and stored-hash assertions in both existing suites. 96 tests and build pass.

- **Rate Limiting for Auth** — Installed `@upstash/ratelimit` + `@upstash/redis`. Created `src/lib/rate-limit.ts`: lazy Upstash client, one sliding-window limiter per endpoint (policy in `RATE_LIMITS` constant), `checkRateLimit(name, identifier)`, `getClientIp(headers)`, and `tooManyRequestsResponse(retryAfterSeconds)`. Fails open when Redis is unconfigured or the call errors. Login limit applied **inside `authorize`** in `auth.ts` (IP+email key, 5/15 min); exceeding it throws a `RateLimitError extends CredentialsSignin` with `code = "rate_limited"`, which the `authenticate` server action catches and returns as `{ error, rateLimited: true }` — rendered as an amber banner in the sign-in form (same style as `OAuthAccountNotLinked`). Register, forgot-password, reset-password (IP-keyed), and resend-verification (IP+email-keyed) each call `checkRateLimit` at the top of their handler and return a real `429` + `Retry-After`; existing client forms surface the error via their `!res.ok` branch. `RATE_LIMITS` table added to `system-constants.ts`. Upstash env vars already in `.env.example`; added a fail-open note. 11 new Vitest tests (107 total); build passes.

- **Homepage Hero Mockup** — CSS 3D cube animation on the public `/` page narrating "Financial Chaos to Clarity" across four phases: scattered grey cubes (chaos) → category columns (structure) → budget progress bars → dashboard reveal. Two strict render paths: animated desktop + motion-OK, static phase-4 for mobile (<768px) and `prefers-reduced-motion`. Web Animations API drives two tracks per cube (compositor-only transform/opacity + decoupled color) so color repaints can't stall motion. Phase-1 float arc (gentle upward bob midway through drift), 280ms intro cross-fade, per-keyframe easing, IntersectionObserver gating with replay on re-enter. Cube bars in phase 3 morph onto measured real budget bar positions (`data-budget-track` hook on `BudgetsPanel`) for aligned morph; falls back to centered bars when measurement fails. Marketing snapshot data (`src/lib/marketing/`) typed against `src/types/dashboard.ts`; copy centralized in `copy.ts` for hero/SEO sync. Page metadata (OpenGraph + Twitter) added. `HOW_IT_WORKS_SECTION_ENABLED = false` routes secondary CTA to `#hero-dashboard` (live anchor) until the features section ships. 11 new Vitest tests for pure choreography (118 total); build passes.

- **Homepage: Full Landing Page** — Extended `/` with a fixed Nav, Features, AI Section, Pricing (monthly/yearly toggle), CTA Banner, and Footer. Fixed nav: transparent over hero → `bg-app/80 backdrop-blur-md` after 8px scroll; mobile Popover API menu; "Get Started" always visible. Features section: 6 data-driven MVP capability cards in a responsive 3-column grid on a `bg-sidebar` alternating band. AI section: two-column layout with "Pro Feature" badge, capability checklist, and decorative auto-tag mock card. Pricing: `useState<BillingPeriod>` toggle, yearly savings % derived from data via `yearlyDiscountPercent()`, two plan cards with green ring on Pro. CTA banner: closing conversion strip → `/register`. Footer: Logo + tagline, link columns (Product, Account), dynamic `new Date().getFullYear()`. Shared `SectionWrapper` (full-bleed band, `raised` prop for `bg-sidebar` alternation) and `SectionHeading` reused across server-rendered sections. Hero updated: h1 enlarged to `text-[40px]/[56px]` with gradient "Clarity" accent (`bg-linear-to-r from-success to-teal-400`), secondary CTA repointed to `#features` (retired `HOW_IT_WORKS_SECTION_ENABLED` flag), preview trimmed to 4 budget rows + 2 goals, `HERO_PREVIEW` const added to `constants.ts`. All copy/data in `src/lib/marketing/` (copy.ts, features.ts, ai.ts, pricing.ts). 8 new Vitest tests for `yearlyDiscountPercent` and `PRICING_PLANS` (126 total); build passes.

- **README Generation** — Replaced the Create Next App boilerplate `README.md` with a professional, 16-section project README. All facts derived from the live repo: versions from `package.json`, env vars mirrored from `.env.example`, folder tree from real `src/`, data model from `prisma/schema.prisma`. Key divergences from the spec (repo wins): shadcn/ui omitted (no `components.json`, `cn()` is hand-rolled, Radix is only transitive); Roadmap reflects actual build state (transactions, budgets, recurring, goals, reports, export, and Stripe are unchecked — those pages/routes don't exist yet). Sections: Hero, Project Status, Screenshots placeholder, Features, Tech Stack (tables with real versions), Architecture (Mermaid diagram), Getting Started, Environment Variables, Database Setup, Running the Project, Project Structure, Authentication, Code Quality, Deployment checklist, Roadmap, Contributing, License. No new tests (documentation-only change); build passes.

- **Transaction List View (Part 1 — Read)** — Server-rendered `/transactions` page with URL-driven filters (type pill, date range, category multi-select, debounced search). Date-grouped feed (Today / Yesterday / `MMM D, YYYY`) using server `now` as anchor. Desktop 4-column table + mobile 2-column card layout. Transfer pairs collapse to one row in all-accounts mode (outflow leg canonical, counterparty backfilled for naming); single-account mode shows the in-account leg with counterparty label. Cursor-based "load more" via read-only server action (`loadMoreTransactions`). Empty states: no data, no results + "Clear filters" link. Suspense skeleton re-triggers on filter change via `key={JSON.stringify(sp)}`. Extracted shared `AppShell` from `DashboardShell`; topbar account selector wired to real accounts with `?account=` URL param. `TYPE_BORDER_COLOR` moved to `src/lib/transactions.ts`. Pure helpers (`buildTransactionWhere`, `collapseTransfers`, `groupTransactionsByDate`, `parseDateParam`, `parseType`) in `src/lib/transactions.ts`. 23 new Vitest tests (149 total); build passes.

- **Transaction Drawer & Edit (Part 2 — Write)** — Wired the full write side of transactions. Promoted the dashboard mock drawer to a shared `src/components/transactions/transaction-drawer.tsx` mounted once in `AppShell` and opened via `useAppShell().openDrawer(editId?)` (create when omitted, edit when an id is passed) — used by both `/dashboard` and `/transactions`. Installed shadcn/ui **Sheet** (Radix Dialog) manually to protect the existing Tailwind v4 `@theme` tokens, plus **Sonner** for the 8-second snackbar undo; `cn()` reconciled to `clsx` + `tailwind-merge` (superset, call sites unchanged); `<Toaster />` mounted in `AppShell`. Drawer is a right panel ≥768px / bottom sheet <768px via `useMediaQuery` + `BREAKPOINTS.mobile`. Six write server actions in `src/actions/transactions.ts` — `createTransaction`, `createTransfer`, `updateTransaction`, `updateTransfer`, `deleteTransaction`, `restoreTransaction` — plus read helpers `getDrawerFormData` (active accounts + categories) and `getTransactionForEdit` (editable pre-fill). All auth-guarded, Zod-validated (`safeParse`), ownership-checked by session `userId`, returning `{ success, data?, error? }` with `revalidatePath("/transactions")` + `revalidatePath("/dashboard")`. User enters a positive magnitude; sign is derived server-side (EXPENSE → negative, INCOME → positive); currency resolved from the account, never client-supplied. Transfers = two `Transaction` rows sharing a `transferPairId`, both `isTransferLeg=true`, opposite signs; transfer **edit** is an atomic delete-and-recreate (soft-delete old pair + create new pair, new `transferPairId`) inside `prisma.$transaction`. Soft delete sets `deletedAt` (both legs for a transfer); `restoreTransaction` clears it, driven by the Sonner Undo action. Zod schemas + inferred types in `src/lib/validations/transaction.ts` (`optionalText` uses `.nullish()`). Shared date helpers extracted to `src/lib/date.ts` (`toDateInputValue`, `todayDateInputValue`, `dateInputToUtc`). Feed rows are now clickable buttons (`onSelect` → edit); `TransactionFeed` splits appended pages into `extraRows` state and re-syncs to the first page when `initialRows` changes after `router.refresh()`. "Add transaction" CTA hidden on mobile (`hidden md:flex`) since the floating + button covers create. `TRANSACTION_TYPE_OPTIONS` added to `constants.ts`; `EditableTransaction`/`DrawerFormData` types added. 21 new Vitest tests covering ownership, sign derivation, two-leg transfer creation, `updateTransfer` atomicity, and delete/restore of both legs (170 total); build passes.

- **Budgets CRUD** — Full read/write stack for `/budgets`. `src/lib/budget-period.ts` (new): `parseMonth`/`parseYear` with safe defaults, `monthBounds` (UTC half-open window, Dec→Jan wrap), `currentPeriod`. `src/lib/validations/budget.ts` (new): `createBudgetSchema` (categoryId, amount, month, year) + `updateBudgetSchema` (amount only); amount capped at `BUDGET_AMOUNT_MAX = 1_000_000`. `src/lib/budget.ts` extended: `mapBudgetRow` (serializable `BudgetListRow` with `icon: string`, resolved client-side), `summarizeBudgets` with injectable `now` and `hasMixedCurrencies` flag; `BudgetSummary` type gained `hasMixedCurrencies: boolean`. `src/lib/db/budgets.ts` (new): `getBudgets` (live spend via half-open month window, EXPENSE only), `getBudgetForEdit`, `getBudgetFormData` (excludes active-budget categories). `src/actions/budgets.ts` (new): `createBudget` (revive-or-create upsert — UX pre-check for active duplicate, then atomic `upsert` keyed on `{userId, categoryId, month, year}`), `updateBudget` (amount only), `archiveBudget`/`unarchiveBudget`, `seedPresetBudgets` (skips any existing row, `createMany({ skipDuplicates })`), thin `getBudgetForEdit` proxy; all auth-guarded, Zod-validated. `src/actions/transactions.ts` touched: `revalidatePath("/budgets")` added to `revalidateTransactionViews`. `src/app/budgets/page.tsx` (new): `force-dynamic`, `Promise.all` for data, `Suspense` keyed on period. `src/components/budgets/*` (new): `BudgetsView` coordinator (period stepper, archive+undo via Sonner), `BudgetList` (summary block + `⚠ Mixed currencies`, progress rows), `BudgetFormDrawer` (shadcn Sheet, right panel ≥768px / bottom sheet <768px, `useTransition`), `BudgetEmptyState` ("Use starter budgets" CTA). Dashboard `BudgetsPanel` "Manage →" wired to `/budgets`. 44 new Vitest tests (budget-period, budget helpers, budget actions) — 214 total; build passes.

- **Recurring Templates** — Full read/write stack for `/recurring`. Partial unique index `RecurringDraft(recurringTemplateId) WHERE status = 'PENDING'` via `--create-only` migration (no schema.prisma change). Pure helpers in `src/lib/recurring.ts`: `advanceNextOccurrence` (month-end day clamping, DAILY/WEEKLY/MONTHLY/YEARLY), `formatCadence`, `isDraftOverdue`, `mapTemplateRow`, `mapDraftRow`. `startOfUtcDay` added to `src/lib/date.ts`. Validation: `createTemplateSchema` (INCOME/EXPENSE only — no TRANSFER), `updateTemplateSchema` (name/amount/cadence only). DB fetchers in `src/lib/db/recurring.ts`: `getTemplates`, `getPendingDrafts`, `generatePendingDrafts` (idempotent, runs on every page load). `src/lib/revalidation.ts` (new plain module): `revalidateTransactionViews()` extracted here because `"use server"` files cannot export non-async functions. Actions in `src/actions/recurring.ts`: `createTemplate` (currency resolved from account, ownership + isArchived check), `updateTemplate`, `pause/resumeTemplate`, `deleteTemplate`, `confirmDraft` (atomic `$transaction` with `updateMany` claim gate, EXPENSE sign via `Decimal.negated()`, `merchant: template.name` stamped on created Transaction), `dismissDraft`. Page: `force-dynamic`, `generatePendingDrafts` before parallel fetch. Components: `RecurringView`, `DraftsInbox` (confirm/dismiss per card, overdue styling), `TemplatesList` (active + paused sections, overflow menu), `TemplateFormDrawer` (Sheet, `useTransition`, edit disables type/account/date/category), `ConfirmDeleteDialog` (native `<dialog>`), `RecurringEmptyState`. Fix: `confirmDraft` stamps `merchant: template.name` so confirmed transactions display the template name (not category or "Transaction") across all views. 42 new Vitest tests (256 total); build passes.

- **Financial Account CRUD** — Full read/write stack for `/accounts`. `src/lib/currency.ts` (new): `DEFAULT_CURRENCY = "EUR"`, `SUPPORTED_CURRENCIES`, `isSupportedCurrency`. `src/lib/account.ts` (new): `deriveBalance`, `mapAccountRow`, `getDefaultActiveAccount`. `src/lib/validations/financial-account.ts` (new): `createAccountSchema` (signed `startingBalance` bounded by `STARTING_BALANCE_MAX`, icon whitelist via `z.enum(ACCOUNT_ICONS)`, no currency — stamped server-side) + `updateAccountSchema` (name/color/icon only). `src/lib/db/accounts.ts` extended: `getAccountsWithBalances` (2-query groupBy — no N+1), `getAccountForEdit`. `src/actions/financial-accounts.ts` (new): `createFinancialAccount` (stamps `DEFAULT_CURRENCY`), `updateFinancialAccount` (name/color/icon only), `archiveFinancialAccount` (atomic `$transaction` also pauses active recurring templates), `unarchiveFinancialAccount` (idempotent, does not auto-resume templates). `confirmDraft` in `src/actions/recurring.ts` gains `isArchived` guard. `getRecentTransactions` in `src/lib/db/dashboard.ts` now filters `financialAccount: { isArchived: false }` (bug fix: archived-account transactions were persisting on dashboard feed). Nav entry points: topbar "Manage accounts" link, UserMenu "Accounts" entry, sidebar bottom utility group (Wallet icon, above Help). Transaction drawer empty state links to `/accounts`. 32 new Vitest tests (288 total); build passes.

- **Goals CRUD** — Full read/write stack for `/goals`. `src/types/goals.ts` (new): `GoalCard`, `EditableGoal`, `ContributionRow`. `src/lib/goals.ts` (new): `isGoalOverdue` (floors both sides to `startOfUtcDay`, strict `<`, so today-due is not overdue), `goalProgressPercent` (clamps `[0, 100]`), `mapGoalRow`, `mapGoalCard`. `src/lib/db/goals.ts` (new, single source of truth): `getGoals` (full, includes contributions desc), `getGoalsSummary` (active-only, feeds dashboard widget), `getGoalForEdit`. `src/lib/revalidation.ts` extended: `revalidateGoalViews()` (revalidates `/goals` + `/dashboard`). `src/actions/goals.ts` (new): `createGoal` (stamps `DEFAULT_CURRENCY`, `currentAmount: 0`), `updateGoal`, `completeGoal` (sole `isCompleted` setter — no auto-complete at 100%), `deleteGoal` (hard delete, cascades to contributions), `addContribution`, `deleteContribution`; atomic `$transaction` for both contribution actions to keep `currentAmount` invariant. `GOAL_COLORS` moved to `constants.ts`; `GOAL_AMOUNT_MAX = 1_000_000` added to `system-constants.ts`. Dashboard `getGoals` + inline overdue math deleted; dashboard page imports `getGoalsSummary` from `db/goals.ts`. Components in `src/components/goals/`: `GoalCard` (progress bar, overdue badge, Overfunded "Over 100%" pill when `saved > target`, overflow menu), `GoalFormDrawer` (Sheet, right panel ≥768px / bottom sheet <768px), `ContributionDrawer` (add contributions + withdrawals, history list with delete), `ConfirmDeleteDialog` (native `<dialog>`, contribution count from already-loaded card data — no extra query), `GoalsView` (active + completed sections, completed dimmed), `GoalEmptyState`. Dashboard `GoalsWidget` "View all →" wired to `/goals`. 37 new Vitest tests in `test/lib/goals.test.ts` and `test/actions/goals.test.ts` (325 total); build passes.

- **Onboarding + Currency Fixes** — Bundled ROADMAP §0 (currency) + §1 (first-run gate). **Part A — currency:** `createBudget`/`seedPresetBudgets` now stamp `DEFAULT_CURRENCY` (EUR) instead of reading `User.preferredCurrency` (drops a DB round-trip and fixes the USD/EUR drift); migration `reconcile_currency_eur_default` flips the `User.preferredCurrency` + `FinancialAccount.currency` schema defaults to `"EUR"` and one-shot-backfills `'USD'`→`'EUR'` across all six currency columns (hand-edited `--create-only` migration; applied to the `development` Neon branch after a read-only pre-flight inventory; production deferred to launch). Discovered `formatCurrency` hard-coded `$` app-wide (the spec wrongly assumed it was already EUR) — fixed `src/lib/format.ts` to `€` and swapped the `$`→`€` input-prefix in the account/budget/transaction/recurring drawers (contribution drawer was already `€`). Added `DEFAULT_ACCOUNT_COLOR`/`DEFAULT_ACCOUNT_ICON` constants shared by the drawer + onboarding (no more `ACCOUNT_*[0]` index coupling). **Part B — onboarding:** `getActiveAccountCount` fetcher + `requireOnboarded`/`redirectIfOnboarded` server guards (derived "onboarded" = `activeAccountCount > 0`, no stored flag); `/onboarding` 3-step flow (inline account form → optional starter budgets → done) as its own centered surface reusing `AuthCard`/`InputFormField`/`SubmitButton` and the existing `createFinancialAccount`/`seedPresetBudgets` actions. Data surfaces (`/dashboard`, `/transactions`, `/budgets`, `/recurring`, `/goals`) swapped to `requireOnboarded()`; `/accounts` + `/profile` stay open as escape hatches; `/onboarding` added to `auth.config.ts` `isProtected`; dashboard gains a defensive `accounts.length === 0` zero-state card. **Fix during QA:** a Server Action's implicit current-route refresh re-ran the reverse guard and ejected the user to `/dashboard` the instant Step 1 created the account, skipping Steps 2–3. Resolved by marking the flow in-progress in the URL (`?step=budgets|done`, `ONBOARDING_STEP_PARAM`) so the reverse guard only bounces *fresh* visits; the account step sets the marker before its mutation and the page derives `initialStep` so reloads resume correctly. Manual QA (Playwright) covered: forward guard, reverse guard, full 3-step happy path with EUR-seeded budgets, and archived-account recovery. Updated `budgets`, `guards`, and `format` tests; 332 tests + build pass.

- **Dashboard Insights Strip** — Actionable pill row on `/dashboard` (ROADMAP §4, confirmed 2026-06-20 after a prior removal). Three fixed signals below the metric strip: **budgets at risk** (≥ 80% spent, amber pill → `/budgets`), **recurring drafts pending** (blue pill → `/recurring`), **overdue goals** (amber pill → `/goals`). Strip renders nothing when all counts are zero. Architecture: at-risk and overdue counts are derived in-process from the page's existing `getBudgetsData` / `getGoalsSummary` arrays (zero extra queries); only the draft count is new (`getPendingDraftCount` — a `count` query in `src/lib/db/recurring.ts`). `BUDGET_AT_RISK_THRESHOLD = 0.8` added to `system-constants.ts`; `DashboardInsights` + `InsightItem` types added to `src/types/dashboard.ts`; pure helpers `countAtRiskBudgets` + `buildInsightItems` (with `plural`) in `src/lib/insights.ts` (reuses `budgetFraction`; NaN/zero-input inputs safely produce `false`). `InsightsStrip` is a server component with a `TONE_CLASS` lookup and `lg:my-1 lg:gap-3` large-monitor breathing room; per-pill KPI cards and a "needs attention" wrapper were considered and rejected (information hierarchy + prior-removal precedent). Dashboard `Promise.all` extended to 7 fetchers; strip inserted between `<MetricStrip />` and the content grid inside the `accounts.length > 0` branch. Freshness already provisioned — all recurring mutations revalidate `/dashboard`. 13 new Vitest tests (345 total); build passes.

- **Reports Page** — Full analytics module for `/reports` (ROADMAP §5). Read-only (no mutations): six server-only fetchers in `src/lib/db/reports.ts` (`getCategoryBreakdown` with one batched category lookup / no N+1, `getMonthlyComparison`, `getAccountBalanceHistory`, `getReportTxCount`, `getReportProfile`, exported pure `reportTxWhere`); cashflow derives from monthly-comparison buckets in-process (no extra query). Pure helpers: `src/lib/report-period.ts` (`parsePeriod`, `periodBounds` half-open UTC `[from,to)` with Dec→Jan wrap, `monthsInRange` — drives x-axis so zero-activity months render — `isPeriodAllowed`, `resolveEffectivePeriod`); `src/lib/reports.ts` (`bucketByMonth`, `reconstructBalanceHistory`, `hasCategoryData`, `hasBalanceData`, `hasEnoughForTrends`, `trendNudgeCopy`). Pro gate: `isPro` read from the DB via `getReportProfile` (not the session); a Free 12m request clamps the query to 3m via `resolveEffectivePeriod`, keeps the 3-month charts rendered, and shows an upgrade banner above the grid — the 12-month query never runs for Free. Four dependency-free SVG charts in `src/components/reports/`: spending-by-category donut, income-vs-expenses grouped bars, cashflow line, account-balance bars — account-balance shipped as the full per-month time-series (no §7.1 fallback taken); negative balances supported (Credit Card / Cash). Per-chart gating: category + balance on data presence; only the two trend charts hold out for `REPORTS_MIN_TRANSACTIONS = 15` with the spec's exact nudge copy. Each `<svg>` is `role="img"` with a data-summarizing `aria-label` (top `ARIA_SUMMARY_MAX = 3` entries then "and N more") + `aria-hidden` on decorative shapes + real-DOM legend; color never the only signal. `UNCATEGORIZED` constant extracted to `constants.ts`, retiring inline copies in `db/dashboard.ts` and `transaction-row.tsx`. New constants: `REPORTS_MIN_TRANSACTIONS`, `REPORTS_FREE_MAX_MONTHS`, `ARIA_SUMMARY_MAX` (system); `REPORT_PERIOD_OPTIONS`, `UNCATEGORIZED` (UI). 35 new Vitest tests (380 total); build passes; no schema change; Playwright QA verified Pro 12m, the Free clamp, account scoping (both params preserved), and the sparse-account nudge.

- **Data Export** — `GET /api/export/csv` and `GET /api/export/json` — the first non-auth API routes (ROADMAP §6). CSV: flat RFC-4180 ledger, UTF-8 BOM + `sep=,` Excel hint (so columns split on double-click in European-locale Excel), formula-injection-safe free-text columns (`escapeCsvTextField`), transfers as two rows. JSON: versioned envelope `{ schemaVersion: 1, exportedAt, data }` (pretty-printed), derived account balances, user-owned categories only, budgets, goals + nested contributions, recurring templates, non-deleted transactions; Decimal→number, `@db.Date`→`YYYY-MM-DD`. Three-layer split: pure helpers `src/lib/export/{csv,json,filename}.ts` → model `src/lib/db/export.ts` (`exportTxWhere`, `getTransactionsForExport`, `getFullExport`, `EXPORT_ENTITY_CLASS`) → thin route glue; ESLint `no-restricted-imports` enforces no Prisma in routes. Auth-guarded (401 JSON, no redirect), `userId`-scoped, tier-agnostic (no `isPro` read, S6); per-user rate limit (`RATE_LIMITS.export`, 10/min, fail-open); unified `{ error, code }` failure contract for 401/429/413 (`tooManyRequestsResponse` gained `code: "rate_limited"`); 10K-tx size cap (CSV `#` marker / JSON 413); empty export valid (D7). `?account=` scoping with the intentional C2 asymmetry (account-bound entities scope; budgets/goals/categories always full). Entry: `<ExportLinks>` on `/accounts` page carrying `?account=`. 37 new Vitest tests (417 total); build passes; no schema change.

- **Settings Page** — `/settings` route with three cards: **Preferences** (display-name edit via `updateProfile` Server Action with 5-second auto-dismissing success banner, email read-only), **Billing** (Free/Pro badge + plan summary from DB, Upgrade/Manage buttons deferred to Stripe slice §8 with HTML comment seam), **Your data** (Data Export relocated from `/accounts`, scope label derived from `?account=` searchParam). Architecture: `getUserOverview` shared projection added to `src/lib/db/profile.ts` (both `/settings` and `/profile` use it — drift prevention); `getAccountLabels` lightweight fetcher in `src/lib/db/accounts.ts` for scope resolution (active + archived, `{id, name, isArchived}` only, no balance computation); `updateProfileSchema` + `PROFILE_NAME_MAX = 80` in `src/lib/validations/profile.ts` / `system-constants.ts`; `PlanBadge` extracted to `src/components/settings/plan-badge.tsx` (imported by both pages). `/settings` is NOT onboarding-gated (escape hatch like `/accounts` and `/profile`); added to `auth.config.ts` `isProtected`. Settings sidebar link added between Accounts and Help. `<ExportLinks>` removed from `/accounts`. Two corrections from spec: `getUserOverview` landed in `profile.ts` (not a new `user.ts`); `getAccountLabels` is new (the spec assumed an active+archived fetcher existed). Fixes: `w-full` on both page containers (width discrepancy between `/profile` and `/settings`); auto-dismiss banner uses `at` nonce from action + `dismissedAt` client state (plain `useState` + Sonner both fail post-action because route refresh resets state / drops toast). 11 new Vitest tests (429 total); build passes.

- **Stripe Billing** — Pro subscriptions (€3/mo, €25/yr) via Stripe Checkout + Customer Portal (ROADMAP §8). `src/lib/stripe.ts`: lazy SDK singleton (`stripe@^22.2.2`, `apiVersion: "2026-05-27.dahlia"`), `STRIPE_PRICE_IDS`, `BillingPeriod`. `src/lib/stripe/events.ts`: pure event→intent mapper (Vitest-safe, no Prisma). `src/lib/db/billing.ts`: `linkCheckout` / `syncSubscription` (updateMany — out-of-order safe) / `clearSubscription` / `reconcileCheckoutReturn`. `src/lib/url.ts`: shared `getBaseUrl()` extracted from email helpers. `src/actions/billing.ts`: `createCheckoutSession` / `createPortalSession`. `src/app/api/stripe/webhook/route.ts`: signature-verified webhook; 500 on handler throw (Stripe retries), 200 on unknown events. `src/components/settings/billing-actions.tsx`: Free upgrade buttons + Pro manage button + `?checkout=success/cancelled` inline banners (no Sonner — /settings renders outside AppShell). Webhook is the only surface that sets `isPro = true` (S2); `isPro` never threaded through JWT — read fresh from DB at every consumption point. Return-side reconciliation closes the webhook/redirect race at first paint. Cancel-on-delete: `softDeleteAccount` cancels the Stripe subscription before stamping `deletedAt`; Stripe errors swallowed (keeps `stripeCustomerId` for audit). `PRICING.currency` `$` → `€`. ESLint `no-restricted-imports` extended to `src/app/api/stripe/**`. No schema migration (columns already existed). 57 new Vitest tests (478 total); build passes.

- **User Category Management** — Full create/edit/delete stack for user-owned categories (ROADMAP §3). No new route — management lives inline in pickers + a "Categories" card on `/settings`. No schema model change — `Category` already had `isSystem`, nullable `userId`, `@@unique([name, userId])`; one `--create-only` migration adds a functional unique index `(lower(name), userId)` for race-proof case-insensitive dedup. `CATEGORY_ICONS` (20-name tuple) + `CATEGORY_COLORS` (12-hex tuple) + `DEFAULT_CATEGORY_ICON/COLOR` added to `constants.ts`. `src/types/categories.ts` (new): `ManageableCategory` (own rows + usage counts) + `EditableCategory`. `src/lib/validations/category.ts` (new): `createCategorySchema` / `updateCategorySchema` (Zod, icon enum whitelist, hex color regex). `src/lib/db/categories.ts` extended: `getManageableCategories` (own rows + `_count` scoped to visible linked records, ordered by name) + `getCategoryForEdit`. `revalidateCategoryViews()` added to `src/lib/revalidation.ts` (touches `/settings`, `/transactions`, `/budgets`, `/recurring`, `/dashboard`, `/reports`). `src/actions/categories.ts` (new, `"use server"`): module-private `assertNameAvailable` (case-insensitive cross-system+own pre-check) + `mapCategoryWriteError` (P2002 → friendly message); `createCategory` returns `CategoryOption` for auto-select; `updateCategory`, `deleteCategory`, `getCategoryForEdit` — all ownership-gated `where: { id, userId, isSystem: false }`; system/foreign rows → `"Category not found."`. `src/components/categories/category-form-drawer.tsx` (new): Sheet, name input + icon grid + color swatches + live `PreviewChip`; `onCreated` prop for auto-select. `src/components/categories/category-picker-field.tsx` (new): wraps `<select>` + "+ New category" button; `appended` state for inline-created rows; `disabled` prop hides the button (recurring edit mode). Swapped into all three drawers: transaction drawer (`emptyLabel="Uncategorized"`), budget form drawer (create branch only), recurring template drawer (`disabled={isEdit}`). `src/components/categories/manage-categories.tsx` + `confirm-delete-dialog.tsx` (new): `/settings` card showing usage line (omits zero clauses) + edit/delete per row; native `<dialog>` confirm with SetNull/Cascade impact copy. `src/app/settings/page.tsx` extended: `getManageableCategories` fetched in `Promise.all`, `<ManageCategories>` rendered between Billing and "Your data". 28 new Vitest tests (506 total); build passes.

- **Pre-Launch Polish** — Final hardening gate before launch (ROADMAP §9). Security/ownership audit confirmed all 40 Server Actions auth-guarded, all 11 `src/lib/db/*` files have `server-only`, join-through ownership correct, ESLint import boundaries and Stripe webhook sig check all holding — no code gaps found. Empty-state audit confirmed all components exist. Code deliverables: `src/app/error.tsx` (client error boundary, `role="alert"`, Try again + dashboard link); `src/app/not-found.tsx` (styled 404, server component); title metadata on all 15 app pages with root layout template `"%s — Spendly"`; `OPENAI_API_KEY` removed from `.env.example` (not read in source); React Compiler advisory ESLint rules downgraded to warn (pre-existing, component refactor is post-launch). Gates: 506 Vitest tests pass, build and lint clean, `prisma migrate status` all applied. Playwright golden-path smoke (sign-in → dashboard → transaction → reports → settings) and breakpoint check at 375/768/1440px. Remaining for launch (operator/manual, not code): §2.4 Stripe live acceptance, §3 full responsive matrix, §8 full a11y pass, §10 cross-browser, §11 backup/DR drill, §12 error monitoring, §12.4 structured audit log, §13 prod env validation (`AUTH_URL`, Google OAuth callback, Stripe live webhook), §14 data-integrity smoke, §15 dependency audit, §16–19 support/runbook/metrics/change-freeze.

- **Fix: Dashboard Dead Controls** — Two markup-only fixes on `/dashboard`. (1) "See all →" in the Recent Transactions panel was a dead `<button>` with no handler; replaced with `<Link href="/transactions">` (same classes, `next/link` import added, component stays a server component). (2) Week / Month / Year period pills in `PageHeader` were backed by a local `useState<Period>` that nothing read — selecting a pill re-styled the button but changed no data; removed per the *"Dashboard is for state, Reports is for analysis"* design principle. Deleted the orphaned constants `PERIOD_OPTIONS`, `Period`, and `DEFAULT_PERIOD` from `system-constants.ts` (confirmed only consumer was `page-header.tsx`). `"use client"` kept on `PageHeader` for the Add button's `onClick`. Sparkline and all fetchers unchanged. No new tests (no logic surface). 506 tests and build pass.

- **Fix: Display Name Doesn't Refresh After Edit** — Sidebar profile row showed a stale name after editing it on `/settings` until sign-out/in. Root cause: JWT session strategy freezes `name` into the token at sign-in; `revalidatePath` re-renders the page but the page re-reads the same stale token. `unstable_update` was attempted first but proved unreliable from a Server Action (cookie write lands one navigation late, producing the characteristic lag-by-one-edit symptom). Switched to the spec's documented fallback: added `getSidebarUser(userId)` — a lean `{ name, email, image }` PK lookup — to `src/lib/db/profile.ts` and folded it into the existing `Promise.all` on all 7 AppShell pages (`/dashboard`, `/transactions`, `/budgets`, `/recurring`, `/goals`, `/accounts`, `/reports`), replacing `session.user.{name,email,image}` at each `user={…}` call site. Also dropped the unnecessary `revalidatePath("/profile")` from `updateProfile` — `profile.ts` reads from the DB on its own render, no revalidation needed from the save hot path. Test updated: `revalidatePath("/settings")` asserted, `"/profile"` asserted absent. No new tests (no new logic surface). 506 tests and build pass. Follow-up spec written: `docs/fixes/budgets-groupby-spend-spec.md` (separate branch, `fix/budgets-groupby-spend`).

- **Fix: Sync README with Shipped State** — The README was written during the README Generation slice when most of the product was unbuilt; the entire MVP (ROADMAP §0–§9) had since shipped. Documentation-only sync on branch `docs/readme-sync`. Changes: (1) Project Status reframed from "transactional surface being built" to "MVP complete; launch-prep tasks remain" — replaced the "In progress" block (Reports/Export/Stripe) with a short "Remaining for launch" operator note; extended the Shipped list with Reports, Data export, Settings, Stripe billing, User categories, Onboarding, Dashboard insights strip, Pre-launch polish. (2) Features section: Reports stated as shipped (four SVG charts, period selector, Free/Pro gate), added Settings and User categories bullets, softened the closing caveat. (3) Tech Stack: Stripe row `—` → `^22.2.2`, "planned" → shipped (Checkout + Portal + webhook). (4) Env vars reconciled with `.env.example` exactly — removed `STRIPE_PUBLISHABLE_KEY` and `OPENAI_API_KEY`. (5) Project Structure: `actions/` generalized to the full 9-file set, `components/` expanded to all 15 real folders, `app/api/*` note includes `export/*` and `stripe/webhook`. (6) Roadmap: flipped Reports/Export/Stripe to `[x]`, added 5 more shipped items, grouped post-MVP under a clear heading. (7) Deployment checklist: added Google prod OAuth callback, Stripe live config, backup/observability. Also committed: `docs/fixes/readme-sync-spec.md` and `docs/fixes/auth-layout-polish-spec.md` (next fix's spec). No Vitest, no build impact.

- **Fix: Auth Card Layout + Scroll-Behavior Warning** — Two markup-only fixes batched into one slice. (1) Removed the standalone `<Logo />` block rendered above `AuthCard` and wrapped the `<h1>` + subtitle `<p>` in a `text-center` div — fixes all four auth pages (`/sign-in`, `/register`, `/forgot-password`, `/reset-password`) at once via the shared component. `import { Logo }` removed (ESLint clean). (2) Added `data-scroll-behavior="smooth"` to `<html>` in `src/app/layout.tsx` alongside the existing `lang` + `className` attributes — silences the Next.js route-transition warning while preserving the `scroll-smooth` Tailwind class for landing-page anchor links. No new tests (no logic surface). 506 tests and build pass.

- **Account Surfaces IA Consolidation (Post-MVP §1)** — Consolidated `/profile` and `/settings` into one coherent account-management surface. `/settings` gains five ordered cards: **Preferences** (compact identity header — avatar, email, "Member since" — above the display-name form), **Security** (new: `<ChangePasswordForm>` gated on `hasPassword` + Sign out button, always rendered), **Billing** (unchanged), **Categories** (unchanged), **Data & privacy** (export controls inside a prominent export-before-delete callout, then `<DeleteAccountDialog>` danger-zone last). `/profile` reduced to `redirect("/settings")` (Next.js 307 — permanent compatibility route, never 404). `UserMenu` "Profile" → **Settings** entry (Settings icon). Dropped `ProfileStats` component + `getProfileStats` fetcher + `ProfileStats` type (D3) and `preferredCurrency` from `getUserOverview` projection (D4 — dormant EUR-only). Moved `change-password-form.tsx` + `delete-account-dialog.tsx` from `src/components/profile/` → `src/components/settings/`; delete dialog gains a one-line export reminder. `ExportLinks` restyled: raised chip (`bg-surface-2` + shadow) + `sm:justify-between` to spread CSV/JSON buttons to opposite ends on desktop. `docs/features/account-settings-ia.md` flipped to "realized" with the canonical Settings IA table + no-new-routes rule. `docs/fixes/settings-nav-dedup-spec.md` written as follow-up (trim duplicate Settings/Accounts from UserMenu drop-up, branch `fix/settings-nav-dedup`). Test update: removed `getProfileStats` suite from `test/lib/db/profile.test.ts`; dropped `preferredCurrency` from the `getUserOverview` projection assertion. 508 tests pass, build + lint clean, no schema change.

- **Fix: De-duplicate Settings / Accounts in UserMenu** — `UserMenu` drop-up previously listed Settings and Accounts links that were already present as persistent sidebar labels directly above it, creating literal stacked duplication. Removed both `<Link>` items from the drop-up; it now contains only the Sign out form button. Dropped the now-unused imports (`Link` from `next/link`, `Settings` and `Wallet` from `lucide-react`). Updated the component doc comment to "…opens a drop-up menu with a sign-out action." Sidebar utility group (`Accounts`, `Settings`, `Help`) is unchanged — those remain the canonical, one-click entry points. `onNavigate` prop retained in the interface (parent passes it) but no longer destructured in the component (Sign out is a form action; wiring `onClick` to close the drop-up was found to cancel the Server Action before it dispatched). No new tests (markup-only). 508 tests pass, build + lint clean.

- **Fix: Budget Spend via `groupBy` Instead of Row-Loading** — Both `getBudgets` (`src/lib/db/budgets.ts`) and `getBudgetsData` (`src/lib/db/dashboard.ts`) were loading every matching EXPENSE row into memory via a nested `budget → category → transactions` Prisma include and summing amounts in JavaScript. Replaced with a single DB-side `transaction.groupBy({ by: ["categoryId"], _sum: { amount: true } })` per period, scoped to `categoryId: { in: budgets.map(b => b.categoryId) }` so only budgeted categories are aggregated (empty budgets → `in: []` → no scan). Negative `_sum.amount` → `Math.abs(Number(...))` for positive `spent`; missing categories fall back to `spentMap.get(id) ?? 0`. `mapBudgetRow` in `src/lib/budget.ts` updated to take precomputed `spent: number` instead of deriving it from `category.transactions`; `MappableBudget.category.transactions` removed. `getBudgetsData` keeps its own `lte: monthEnd` window and `resolveIcon` row build (does not call `mapBudgetRow`). Tests: `test/lib/budget.test.ts` updated to new `(budget, spent)` signature; new `test/lib/db/budgets.test.ts` asserts groupBy where-shape, negative→positive spend, zero-spend fallback, and `in: []` guard. No behavioural change. 510 tests pass, build and lint clean.

- **Help / FAQ Route (Post-MVP §2)** — Stood up `/help`, retiring the dead sidebar Help link (was a hard 404). Static, server-rendered FAQ inside `AppShell` — auth-guarded via `getSessionOrRedirect`, **not** onboarding-gated (escape hatch like `/accounts`/`/settings`). Content is a typed module `src/lib/help/content.ts` (`HELP_SECTIONS` — 10 sections: Getting Started, Common Questions, Accounts, Transactions, Budgets, Recurring, Goals, Reports, Categories, Data & Privacy — each with a Lucide `icon` + hex `color`; `HelpItem`/`HelpSection` interfaces). Renderers in `src/components/help/` (`help-content.tsx` + `help-section.tsx`): per-section tinted icon squares (matching the dashboard `CategoryIcon`/`MetricStrip` pattern), a header icon square, stacked term/detail explainer lines, and a prominent green-accented **"On this page"** TOC gated on `HELP_SECTIONS.length >= HELP_TOC_MIN_SECTIONS` (`= 5` in `system-constants.ts`). `/help` added to `isProtected` in `auth.config.ts`; sidebar Help link gained the Settings-style active-state highlight. No DB, no mutations, no schema change. Spec: `docs/features/help-faq-route-spec.md`. POST-MVP-ROADMAP §2 marked shipped; `/help` route row added to `project-overview.md`. `test/lib/help/content.test.ts` structural invariants (non-empty, unique anchor ids, non-empty details, icon + hex color). 514 tests pass, build + lint clean.

- **AI Auto-Categorization (Pro) + AI Foundation (Post-MVP §3)** — First AI feature; stands up the shared `src/lib/ai/` foundation reused by §4–§6/§10 v2. A Pro-only **"Suggest"** button beside the Category field in the transaction drawer (light-blue `--color-ai` accent) calls an LLM that pre-selects the best-matching category and offers an opt-in **merchant-cleanup** chip — **suggestion only; `createTransaction`/`updateTransaction` stay the sole writers, no new write path; fail-open to the manual picker on any error.** Foundation: `lib/ai/client.ts` (lazy OpenAI singleton, mirrors `stripe.ts`), `lib/ai/respond.ts` (`aiJsonRespond` Responses-API wrapper — model behind `AI_MODEL`, default `gpt-5-nano`; Chat Completions returns empty for this model; wrapper also guarantees the literal word "json" is in the `input`, which the `json_object` guardrail requires — the live bug that produced the generic fail-open until fixed), `lib/ai/run.ts` (`runAiFeature` — the single spine: auth → **DB-driven Pro gate** `getAiProfile`, never the JWT → `aiMonthly` 500/30d global per-user COGS rail + `aiSuggest` 20/h per-feature burst → 8s AbortSignal timeout → **exactly one `ai_result`** telemetry event → **fail-open, never throws**), `lib/ai/errors.ts` (`AiParseError`/`AiNoMatchError` sentinels), `lib/db/ai.ts` (`getAiProfile`), `lib/analytics/track.ts` (no-op telemetry shim; §0 swaps the body later). Feature: `actions/ai/suggest-category.ts` (thin over `runAiFeature`; **`no_match` degrades to `categoryId: null`, not an error**), `actions/ai/track-outcome.ts` (accept/override telemetry from the client drawer), versioned prompt `lib/ai/prompts/category.ts` (`CATEGORY_PROMPT_VERSION = 1`), pure `lib/ai/category.ts` (`parseSuggestionJson` + `matchCategoryByName` — case-insensitive, never invents an id), `lib/validations/ai.ts`. UI: `isPro` threaded through `getDrawerFormData` (extra parallel `getAiProfile`); `DrawerFormData` gained `isPro`; the `Field` component gained an `action` slot; Suggest button gated on free-text input (`hasSuggestInput`) with a `suggestRunRef` session-token guard so a slow suggestion can't land on a reopened drawer; low-confidence hint only when a category was actually pre-selected. Constants: `AI_MODEL`, `AI_INPUT_MAX_CHARS`, `AI_TIMEOUT_MS`, `RATE_LIMITS.aiSuggest`/`aiMonthly` in `system-constants.ts`; `--color-ai`/`--color-ai-strong` in `globals.css`. **Deviations from spec (repo wins):** model is `gpt-5-nano` (not `gpt-4o-mini`); reused `getUserCategories` rather than a new `getSuggestableCategories`; truncation owned by the action's `clip()` (over-long input truncated, never rejected — schema `.max` dropped to honor "truncated regardless"). Docs: POST-MVP §3 + Delivery Sequence marked shipped; `project-overview.md` Design System AI-accent row + Out-of-Scope "silent auto-tagging" reframing. 30 new Vitest tests (`test/lib/ai/category.test.ts`, `test/lib/ai/run.test.ts`, `test/actions/ai/suggest-category.test.ts`) — 540 total pass, build + lint clean. No schema change. Spec: `docs/features/ai-auto-categorization-spec.md`.

- **Natural-Language Quick Capture (Pro) (Post-MVP §4)** — Second AI feature; the first proof of §3's "prompt + parse step only" reuse claim. A Pro-only **"Quick add"** natural-language field at the top of the **create-mode** transaction drawer (light-blue `--color-ai` accent, Sparkles): a Pro user types one line — `"12 lunch at Pret yesterday"` — and the new `parseTransaction` action pre-fills type/amount/date/category/merchant/note for confirmation. **Suggestion only; `createTransaction` stays the sole writer — no new write path; fail-open to manual entry on any error.** Reuses the §3 foundation **unchanged** (`runAiFeature`, `aiJsonRespond`, `matchCategoryByName`, `getUserCategories`, `getAiProfile`, `track`, `AiNoMatchError`/`AiParseError`, `--color-ai`, `AI_INPUT_MAX_CHARS`, `RATE_LIMITS.aiSuggest`/`aiMonthly`). New: `lib/ai/prompts/transaction.ts` (`PARSE_INSTRUCTIONS` v2 — currency-word amount extraction + food/store→Groceries category inference + worked example, `buildParseInput`, `TRANSACTION_PARSE_PROMPT_VERSION = 2`); pure `lib/ai/parse.ts` (`parseDraftJson` — defensive coerce, amount coerces numeric strings including `"15 euro"`, type → EXPENSE unless exactly "INCOME", confidence → "low" unless exactly "high"; `resolveDraftDate` — keep literal `YYYY-MM-DD` else today; `ParseConfidence` + `ParsedDraft` declared here); thin `actions/ai/parse-transaction.ts` (over `runAiFeature`: `feature: "transaction_parse"`, `burstLimit: "aiSuggest"` — own bucket, no new `RATE_LIMITS` entry; **`amount == null` → `AiNoMatchError` → `reason: "no_match"`**; `clip()` truncates to `AI_INPUT_MAX_CHARS`); `actions/ai/track-parse-outcome.ts` (`ai_parse_confirmed` with `edited`/`edited_field_count`/`edited_fields` — field **names** only). `parseTransactionSchema` added to `lib/validations/ai.ts`. UI (`transaction-drawer.tsx`): Quick add field gated on `!isEdit && isPro`; `quickAddText` retained for session (D7); wholesale re-parse + snapshot reset (D8); account never auto-set (D4); `parseRunRef` stale-result guard; **autofocus on desktop create-open** (`autoFocus={isDesktop}`, D10); low-confidence hint; `reason`-driven fail-open notes; `draftSnapshot` diffed on Save → `trackParseOutcome`. **Foundation latency/quality fix:** `gpt-5-nano` default `medium` reasoning effort took ~9–11s (past `AI_TIMEOUT_MS = 8000`), failing calls open intermittently. Added `AI_REASONING_EFFORT = "low"` (wired into `aiJsonRespond`) — `low` matches `medium` category quality at ~2–3s; raised `AI_TIMEOUT_MS` 8000→12000. Benefits §3 category-suggest too. **Deviation:** `ParseConfidence` declared in `parse.ts`, not re-exported from the `"use server"` action (Turbopack emits `export type { … }` as a runtime binding → `ReferenceError`). No schema change, no migration, no new client. 18 new/updated tests — 558 total pass, build + lint clean. Spec: `docs/features/nl-quick-capture-spec.md`.

- **Budget Rollover (Post-MVP §7)** — Opt-in per-budget **"Roll over remainder"** toggle. When on, a budget's **effective limit** = `base limit + prior month's remainder` (`effective − spent`), carried while the budget stays rollover-on across consecutive calendar months. Carry is **positive** (underspent → more room) or **negative** (overspent → less room). **Derive on read:** `resolveRolloverCarry` walks back the consecutive rollover run (bounded by `ROLLOVER_MAX_LOOKBACK_MONTHS = 24`), trims at the first gap/rollover-off month, sorts chronologically, folds via pure `rolloverCarryIn`, and rounds once at the boundary — no stored carry, no cron. **Chain rule:** a gap month or rollover-off month resets the run; the next rollover-on month starts fresh from its base. **Single edge source:** `budgetProgressWithCarry(spent, baseLimit, carriedAmount)` owns the `effectiveLimit ≤ 0 → danger/100%` case; the `/budgets` row, dashboard panel, and `countAtRiskBudgets` all route through it. Carry copy direction-worded by one shared `formatCarry` helper (`↻ +€100 rolled over` / `↻ €70 overspent last month`). Per-budget `Boolean @default(false)` flag (migration `add_budget_rollover` — pure additive). Not a Pro gate; free for all users. New: `src/lib/rollover.ts` (`rolloverCarryIn`, `effectiveLimit`, `RolloverPoint`); `previousPeriod` in `budget-period.ts`; `ROLLOVER_MAX_LOOKBACK_MONTHS` in `system-constants.ts`. Extended: `budget.ts` (`roundMoney`, `budgetProgressWithCarry`, `formatCarry`, `mapBudgetRow`, `summarizeBudgets` → effective totals); `db/budgets.ts` (`resolveRolloverCarry` + `getBudgets`/`getBudgetForEdit` wiring); `db/dashboard.ts` (shared carry); `insights.ts` (effective-limit at-risk count); `validations/budget.ts` + `actions/budgets.ts` (rollover threaded through create/update/seed); `budget-form-drawer.tsx` (toggle + worked example); `budget-list.tsx` + `budgets-panel.tsx` (effective limit display + carry line). Two static `BudgetRow[]` literals (`marketing/dashboard-snapshot.ts`, `mock-data.ts`) updated to satisfy the new required fields. Docs: help "No rollover" item rewritten; `project-overview.md` schema mirror + feature note + Out-of-Scope reframe; `POST-MVP-ROADMAP.md` §7 + delivery row 5 marked shipped. 587 tests pass (29 new in `rollover.test.ts` + extensions to budget/period/action/db/insights suites), build + lint clean. Spec: `docs/features/budget-rollover-spec.md`.

- **Trash UI (Post-MVP §8)** — `/trash` "Recently deleted" recovery surface for soft-deleted transactions. Per-row **Restore** (reuses `restoreTransaction` unchanged) and **Delete forever** (new `hardDeleteTransaction`), plus **Empty trash** bulk action (`emptyTrash`) — both permanent deletes confirm-gated. No schema change: reuses `Transaction.deletedAt` + `@@index([deletedAt])`; new `getDeletedTransactions` (all-accounts, newest-first, `collapseTransfers` for transfer pairs, no archived exclusion) + `getDeletedTransactionCount` in `src/lib/db/transactions.ts`; `TrashTransaction extends FeedTransaction` in `src/types/transactions.ts`; pure `formatDeletedAt` helper ("Deleted 3 days ago · Jun 25"). Entry: "Recently deleted · N" count badge in `transactions-header.tsx` (suppressed at 0), refreshed by the existing `revalidateTransactionViews()` (extended to touch `/trash`). Components in `src/components/trash/`: `TrashView` (coordinator — rolling-count Sonner toasts per action kind, `router.refresh()` after each mutation, "Empty trash" hidden when list is empty), `TrashRow` (type-border + category icon + deletion label + Restore / Delete forever), `ConfirmDeleteDialog` (native `<dialog>`, transfer-aware copy), `TrashEmptyState`. `/trash` added to `auth.config.ts` `isProtected`. Help copy flipped from "no Trash view"; docs: `project-overview.md` route row + Out-of-Scope graduation + architecture note; `POST-MVP-ROADMAP.md` §8 + delivery row 6 marked shipped; `entity-types.md` + `entity-crud-architecture.md` updated. No Pro gate, no cron/auto-purge, transactions only. 18 new tests — 605 total pass, build + lint clean. Spec: `docs/features/trash-ui-spec.md`.

- **Data Import (Post-MVP §15)** — `/import` migration counterpart to export: upload a **CSV** (column-mapping step) or a **Spendly JSON export** → **preview** → **confirm**; nothing is written until the user confirms a dry-run preview. Three Server Actions in `src/actions/import.ts` (`inspectCsv` / `previewImport` / `commitImport`) — **not** an API route (a file *upload* can be a Server Action; export's route rationale is download-only). Both formats funnel through one pure pipeline (`src/lib/import/*`): `csv.ts` (dependency-free RFC-4180 state machine + `stripBomAndSepHint` + majority-vote dialect detection for delimiter/decimal/date, per-cell `IMPORT_MAX_CELL_CHARS` truncation), `mapping.ts` (index-based `suggestMapping`/`applyMapping`, duplicate-header-safe), `parse.ts` (`parseDateFlexible`/`parseAmount`/`resolveType` + `normalizeCsvRow`/`normalizeJsonRow`, D9 text rules), `resolve.ts` (`normalizeCatKey` trim→collapse→NFC→lower, `buildCategoryIndex`, `resolveCategory`), `dedup.ts` (`dedupKey` excluding category + `partitionForWrite` count-based multiset), `json.ts` (`parseImportEnvelope` strict `schemaVersion`/lenient unknown fields, distinct structural errors). DB: `src/lib/db/import.ts` (`getImportTargets`, `countExistingForDedup` — bounded findMany, `userId`-scoped). **One target account** (C1, archived rejected); INCOME/EXPENSE only, transfers skipped+counted (D1); signed amount from type + currency stamped from account (D2); date `@db.Date` UTC midnight via `dateInputToUtc` (D3); **count-based dedup** keyed `(date, signedAmount, type, merchant, note)` so re-import is idempotent (D4); tolerant+partial (row failures skip, structural failures = whole-import error, D5); preview==commit with `divergedFromPreview`/`expectedCreate` (D6); atomic `$transaction` `createMany` categories-then-transactions with race-safe re-query (D7); no schema change/provenance column (D8). Tier-agnostic (no `isPro` read, S6); per-user rate-limited (`RATE_LIMITS.import`). UI: `/import` `AppShell` page (auth-guarded, not onboarding-gated, zero-account empty state) + `ImportFlow`/`ColumnMapper`/`ImportPreview`; Settings → Data & privacy “Import data” card (info-blue, matches Export card); `/import` added to `auth.config.ts`. Single-sourced field caps `MERCHANT_MAX`/`NOTE_MAX`/`CATEGORY_NAME_MAX` to `system-constants.ts` (validations refactored to use them). New constants: `IMPORT_MAX_ROWS/FILE_BYTES/PREVIEW_SAMPLE_SIZE/MAX_ISSUES/MAX_CELL_CHARS/HIGH_SKIP_RATIO` + `RATE_LIMITS.import` (system); `IMPORT_FIELDS`/`IMPORT_DATE_FORMATS`/`CATEGORY_RESOLUTION_OPTIONS` (UI). Types in `src/types/import.ts`, validation in `src/lib/validations/import.ts`. **Two edge-case bugs found via hard fixtures + fixed:** (1) `detectDateFormat` required *every* sample to match → one junk row poisoned detection to ISO; now majority-vote. (2) Zod 4 treats `z.unknown()` as *required* → a JSON row missing a field rejected the whole file; now all tx fields optional so a malformed row is an invalid *row* (D5), not a structural fail. Docs: `POST-MVP-ROADMAP.md` §15 + delivery row 7 shipped; `project-overview.md` `/import` route + Data Portability import note; `entity-crud-architecture.md` import-as-Server-Action note; `/help` Data & privacy item. 109 new tests — 700 total pass, build + lint clean. Spec: `docs/features/data-import-spec.md`.

- **Fix: Unified Import Drop Zone** — Replaced the CSV/JSON **format toggle** + click-only file picker on `/import` with a single **drag-and-drop drop zone** that accepts either type and **auto-detects the format from the filename extension** (click-to-browse retained; an unrecognized extension is rejected inline). UI polish only — no schema, migration, Server Action (`inspectCsv` / `previewImport` / `commitImport`), or `src/lib/import/*` pipeline change; every downstream step already keys off `format`, which is now *derived* client-side instead of *chosen*. New pure helper `detectImportFormat` in `src/lib/import/format.ts` (extension-only, case-insensitive, whitespace-tolerant; `.csv` → `"csv"`, `.json` → `"json"`, else `null` → `"Unsupported file — drop a .csv or .json file."`). `import-flow.tsx`: toggle block removed; `format` derived in `handleFile` (detection prepended, unknown rejected); `reset()` dropped its `nextFormat` arg; `isDragging` state + HTML5 drag handlers added (`onDragOver` `preventDefault()`; drag-active highlight); permissive `accept=".csv,.json,text/csv,application/json"`; copy + doc comment generalized. Also aligned the `/import` content width to `/settings` (`max-w-2xl` → `max-w-lg`) while **keeping `AppShell`** — classified by interaction complexity (a multi-step upload→configure→preview→confirm workflow benefits from the persistent sidebar as an escape route) rather than navigation context (launched from Settings). Docs: fix spec `✅ Shipped` banner; `data-import-spec.md` UI flow rewritten (toggle → drop zone, steps renumbered). 5 new tests in `test/lib/import/format.test.ts` — 705 total pass, build + lint clean. Spec: `docs/fixes/import-drop-zone-spec.md`.

- **Fix: Delete Account as a Data & Privacy Inner Card** — Restyled the **Delete account** block on `/settings` from its own top-level `<section>` into a **third, danger-tinted inner card inside the Data & privacy section**, sitting last after the export (green) and import (blue) callouts — so the three privacy actions read as one group rather than a separate bolted-on tier. UI polish only (markup/classes), two files. `delete-account-dialog.tsx`: root `<section className="rounded-xl border border-danger/30 bg-surface p-6">` → `<div role="group" className="rounded-lg border border-danger/30 bg-danger/10 p-4">` (matches the export/import inner-card `rounded-lg … p-4` footprint, danger-tinted `bg-danger/10`), `aria-labelledby="delete-account-heading"` kept; `<h2>` restyled `text-[13px]` → `text-[12px] font-medium text-danger` — deliberately **kept as a real `<h2>`** (not demoted to `<p>`) so it retains the accessible name + document-outline entry while borrowing only the callouts' `text-[12px] font-medium` size/weight; `text-danger` retained so it still reads destructive. Trigger button, native confirm `<dialog>`, and all handlers untouched. `settings/page.tsx`: moved `<DeleteAccountDialog email={user.email} />` from its top-level sibling position into the **end of the Data & privacy `<section>`**, after the import callout, wrapped in `<div className="mt-4">` for the same export→import gap rhythm; replaced the stale `{/* Danger zone — kept last (§3 D7) */}` top-level comment with an inline note on the card so the D7 "danger last" rationale is preserved. **Behaviour unchanged:** confirm dialog, typed-email match gate, `deleteAccount` action, `ACCOUNT_DELETION_GRACE_PERIOD_DAYS` copy, and the in-dialog "export from Data & privacy above" reminder. No schema change, no migration, no Server Action change, no new constant, no logic change, no prop/import change. No new Vitest (components are not unit-tested per project standards). 705 tests pass, build + lint clean (0 errors; pre-existing warnings only). Spec: `docs/fixes/settings-delete-account-card-spec.md`.

- **Transaction Tags (Post-MVP §16)** — Free-form, user-owned labels **orthogonal to categories** (`vacation-2026`, `reimbursable`) — a transaction can carry several. First slice since the MVP to add a **new schema model**; supersedes the parked Category Hierarchy (§12). Two additive Prisma models — `Tag` (`id`, `name`, `color?`, `userId`, `createdAt`; **no `isSystem` — every tag is user-owned**) + `TransactionTag` many-to-many join (**composite PK `@@id([transactionId, tagId])`**, no surrogate id) — via migration `add_tags`, plus a `--create-only` `tag_name_ci_unique` functional `(lower(name), userId)` unique index (verified present on the `development` Neon branch); **no existing table changed**. Backend: `validations/tag.ts` (create/update; optional free-hex color, nullish to clear) + `tagIds` threaded into `createTransactionSchema` (bounded `TAG_MAX_PER_TRANSACTION`, lenient default `[]`); `db/tags.ts` (`getUserTags` / `getManageableTags` with non-deleted `_count` / `getTagForEdit`); `FEED_INCLUDE` + `toLeg` include tags **name-sorted**; `buildTransactionWhere` gains a **match-any (OR)** `tags.some.tagId.in` clause + tag-name search branch (**deliberately NO `TRANSFER` guard**, unlike the category filter); `collapseTransfers` passes tags through (transfer rows → `tags: []`). `actions/tags.ts` (`createTag` returns the persisted `TagOption` for picker auto-select / `updateTag` / `deleteTag` / `getTagForEdit`; three-layer case-insensitive dedup — app pre-check + functional index + `P2002` catch, minus the category system-collision layer; ownership `where: { id, userId }`). `createTransaction`/`updateTransaction` **stay the sole writers** of join rows: all-or-nothing `tag.count` ownership check + `new Set` dedup, atomic `$transaction` (create + `transactionTag.createMany`; update = replace with an **unchanged-set skip** — normalized-set equality no-ops the join write, else `deleteMany`→`createMany`). `revalidateTagViews()` (`/settings` + `/transactions` + `/dashboard`; not `/budgets`/`/reports`). UI: `components/tags/` — `tag-chip` (shared chip + `TagChipRow` overflow), `tag-form-drawer` (Sheet: name + `TAG_COLORS` swatches + "no color" + live preview), `tag-picker-field` (**search-first** multi-select combobox + "+ New tag" auto-select + client-side cap), `manage-tags` + `confirm-delete-dialog` (one-liner copy — joins cascade, transactions survive, no undo). Wired into the transaction drawer (Tags field, income/expense only), feed rows (chips, max `TAG_CHIPS_VISIBLE_MAX = 3` + `+N`), the `/transactions` filter bar (id-based `?tag=` pill resolved against the known list so deleted ids drop silently), and a "Tags" card on `/settings` after Categories. New constants: `TAG_COLORS` / `DEFAULT_TAG_COLOR` (`constants.ts`); `TAG_NAME_MAX = 32` / `TAG_MAX_PER_TRANSACTION = 12` / `TAG_CHIPS_VISIBLE_MAX = 3` (`system-constants.ts`). Types in `src/types/tags.ts` + `FeedTag`/`tagIds`/`tags` on transaction types. **Not Pro-gated, no count limit.** **Deferred (documented seams):** Reports tag-breakdown chart; **export/import of tags** (a JSON round-trip loses tag *associations* in v1 — deliberate scope call, not a gap); tags on transfers; match-all (AND) filter; MRU/manual ordering; bulk-tagging; AI tag suggestion; picker virtualization. **Deviation:** feed-row chips render on the full `/transactions` feed only — the dashboard's compact recent-transactions list (separate `TransactionRow` type/component) keeps its existing form; `TAG_CHIPS_VISIBLE_MAX` stays the shared constant for when dashboard chips are added. 36 new tests (`test/actions/tags.test.ts`, `test/lib/validations/tag.test.ts` + extensions to `transactions`/`db.transactions` suites) — **741 total pass**, build + lint clean (0 errors; pre-existing advisory warnings only). Spec: `docs/features/transaction-tags-spec.md`.

- **Split Transactions (Post-MVP §17)** — One **expense** split across multiple categories (€80 shop = €55 Groceries + €25 Household). Stays **one row / one amount** (derived balances provably unaffected) while its spend is **attributed across categories** for budgets + Reports. Additive `TransactionSplit` child model (`id`, `amount Decimal(12,2)`, `note?`, `transactionId` Cascade, `categoryId?` SetNull; two indexes) via plain migration `add_transaction_splits` — **no existing-table change, no `Transaction.isSplit` column** (split status **derived** from child-row presence), no CHECK, no backfill. **EXPENSE-only, single-account, not Pro-gated.** **Aggregation (the load-bearing change):** new server-only `getCategorySpend` (`src/lib/db/split-spend.ts`) does a two-`groupBy` union — non-split rows (`splits: { none: {} }`) + split lines through the parent relation, merged additively, `Math.abs`-ed, returning `Map<string | null, number>` (null key = Uncategorized for Reports; budgets pass `categoryIds` so null never surfaces) — so **double-counting is structurally impossible**. Window passed verbatim (helper is window-agnostic: `/budgets` half-open `lt`, dashboard inclusive `lte`). Rewired `getBudgets`, **`resolveRolloverCarry`** (the easy-to-miss past-month reader), `getBudgetsData`, and Reports `getCategoryBreakdown`; `getMonthlyComparison`/balances/`getReportTxCount` bucket by amount+date and are untouched. `countAtRiskBudgets` inherits split spend for free. **Write path:** `createTransaction`/`updateTransaction` stay sole writers — `resolveOwnedSplitCategories` all-or-nothing ownership (`category.count`, mirrors `resolveOwnedTagIds`), parent `categoryId` nulled when split, split rows written via `createMany` inside the existing atomic `$transaction` with tags; update **replaces** lines (`deleteMany`→`createMany`, no unchanged-set optimization) and toggles split-mode both directions; `getTransactionForEdit` re-hydrates `EditableTransaction.splits`. Transfers untouched. **Validation:** `splits` array + first `.superRefine` in the codebase (EXPENSE-only, ≥`SPLIT_MIN_LINES`, sum-to-total to the cent, mutually exclusive with top-level `categoryId`); `round2` extracted to shared `src/lib/money.ts` (used by validation + actions). **Feed:** `FEED_INCLUDE`/`toLeg` carry `splits` (`amount desc`) + derived `isSplit`; `collapseTransfers` passes through (transfer → `isSplit:false`). **UI:** drawer "Split" toggle (EXPENSE-only, in the Category `Field` action slot, hides AI Suggest in split mode) swaps the single picker for `SplitEditor` (per-line `<CategoryPickerField>` + cent amount + note, "+ Add split" capped, live running total via `formatCurrencyCents`, **"Distribute remaining"**, Save gated on `isSplitBalanced`); pure sum/gate/distribute logic in `src/lib/split.ts`. **Feed row restructured** from a single `<button>` to a container with an inner edit `<button>` + a sibling **disclosure `<button>`** (`aria-expanded`/`aria-controls`, caret `aria-hidden`) revealing the lines — non-split rows behave identically. Dashboard recent list shows a `Split · N` label (detected via `_count.splits`; not expandable) so split parents don't read "Uncategorized". Settings category-delete copy notes split lines fall back to Uncategorized (SetNull). **Export:** JSON envelope bumped `1 → 2` with nested `splits` + derived `isSplit`; CSV keeps a split as one `Split`-labelled row; import accepts v1 & v2 but **ignores `splits` in v1** (round-trip flattens to Uncategorized — documented top post-release follow-up). Feed search over split-line categories/notes also deferred. New: `src/lib/{money,split}.ts`, `src/lib/db/split-spend.ts`, `src/components/transactions/split-editor.tsx`; `formatCurrencyCents` in `format.ts`. Constants: `SPLIT_MIN_LINES=2`/`SPLIT_MAX_LINES=20`/`SPLIT_NOTE_MAX=120` (system), `SPLIT_LABEL`/`SPLIT_ICON` (UI); `EXPORT_JSON_SCHEMA_VERSION` `1→2`. Types: `FeedSplit` + `isSplit`/`splits` on transaction + export types. **Refinements beyond spec:** added `/reports` to `revalidateTransactionViews()` (was missing; split spend now moves the breakdown); added `formatCurrencyCents` (spec said reuse `formatCurrency`, which drops decimals and would misreport "€0.40 left" as "€0"); relaxed the JSON import version check to a `1..CURRENT` range so old exports still import. **63 new/updated tests** (`money`, `split`, `validations/transaction`, `db/split-spend`, `db/split-invariance` new; `db/budgets`, `insights`, `actions/transactions`, `db/export`, `export/csv`, `import/json`, `actions/import`, `lib/transactions` extended) — **785 total pass**, build + lint clean (0 errors; pre-existing advisory warnings only). Migration applied to the `development` Neon branch (`prisma migrate status` clean). Spec: `docs/features/split-transactions-spec.md`.

- **Monthly Review Narrative (Pro, AI) (Post-MVP §5)** — First **insight** AI feature: a Pro-only "Generate summary" card on `/reports` that phrases **this calendar month vs last** into 1–4 plain-language sentences (*"Transport budget is over the limit by 40. Pets spending dropped from 130 to 0. Net cashflow +€915."*). **Read-only, fail-open, on-demand (D1)** — the charts always render regardless; Free users see nothing and no AI call fires. **Load-bearing rule (D2): the model only phrases pre-computed facts; it never computes or invents a number.** A deterministic pure `buildReviewFacts` (`src/lib/reports-review.ts`) owns **every** figure — movers ranked by absolute euro change (`previous == 0` → `direction: "new"`/`deltaPct: null`, never `Infinity`; disappeared category → `down`), a `REVIEW_MIN_MOVER_DELTA = 5` floor dropping trivial swings **before** ranking (D8), over/under budget notes against the **effective** (rollover-adjusted) limit via `effectiveLimit()`, a fixed `topInsight` lead by kind-priority (over-budget > largest mover > cashflow > null, D7), name-ascending tie-break, `round2` throughout. A pure `validateReviewNumbers` (`src/lib/ai/review.ts`) then **drops any generated line whose numbers aren't in the facts** (money within `NUMERIC_GUARD_MONEY_EPSILON = 0.01`, pct within `NUMERIC_GUARD_PCT_EPSILON = 1`; separator/€-tolerant token normalization; all-dropped → `AiParseError` → fail open). Figures reuse the split-aware `getCategorySpend` + rollover-aware `getBudgets` so the narrative agrees with the charts and `/budgets` bars; income/expense totals via a scoped `groupBy(["type"])`. **Fixed month-over-month window** (`currentPeriod`/`previousPeriod` + `monthBounds`), honors `?account=` (same `reportTxWhere` scope rule), **ignores the period pills** (D3). Thin `generateMonthlyReview` action (`src/actions/ai/monthly-review.ts`) over `runAiFeature` (`feature: "monthly_review"`, `burstLimit: "aiSuggest"` — **own bucket under the shared 20/h policy, no new `RATE_LIMITS`**); `hasReviewSignal` false → `AiNoMatchError` → `reason: "no_match"` → quiet "Not enough data yet to summarize this month." **Reuses the §3 AI foundation unchanged** (`runAiFeature`/`aiJsonRespond`/`getAiProfile`/`track`/`AiParseError`/`AiNoMatchError`/`--color-ai`) — prompt + parse + facts step + card only; **no new client, orchestrator, Pro read, rate policy, telemetry sink, color, schema, or migration** (second proof of §3's D6 after §4). Versioned prompt (`MONTHLY_REVIEW_PROMPT_VERSION = 1`). Telemetry: the one `ai_result` from the orchestrator + an `ai_numeric_guard { feature, prompt_version, dropped_count, kept_count }` (counts only, no PII) emitted **only** when the guard drops ≥1 line. **UI:** rendered as a **standalone AI-accented section** (`border-ai/25` + Sparkles header square, **not** a `ChartCard`) with a **fixed self-describing "This month vs last month" subtitle** and the concrete month as an in-body eyebrow — deliberately pulled out of the period-scoped chart grid and gated on **`isPro` only** so it's period-independent (chosen over "show only on the 1-month pill"); `useTransition` loading, "Generated just now" relative timestamp (D10), Regenerate, `reviewRunRef` stale guard, `reason`-driven fail-open copy; account scope arrives as a **prop** (page Suspense `key` remounts on account change, clearing a stale narrative) rather than `useSearchParams`. **Deviations from spec (repo wins):** `AiResult` failure field is `error` not `message`; standalone AI section instead of `ChartCard`; the data-thin gate is `isPro`-only (the spec's `hasEnoughForTrends(txCount)` was period-scoped and wrongly hid the card on the 1-month pill); the numeric guard allows both signed and absolute magnitudes so direction-word prose ("€15 over") stays faithful. New: `src/lib/reports-review.ts`, `src/lib/ai/review.ts`, `src/lib/ai/prompts/review.ts`, `src/lib/db/monthly-review.ts`, `src/actions/ai/monthly-review.ts`, `src/components/reports/monthly-review.tsx`. Constants: `REVIEW_MIN_MOVER_DELTA` (system) + `monthlyReviewSchema` (validations/ai). **38 new tests** (`test/lib/reports-review.test.ts`, `test/lib/ai/review.test.ts`, `test/actions/ai/monthly-review.test.ts`) — **823 total pass**, build + lint clean (0 errors; pre-existing advisory warnings only). Verified live on the `development` Neon branch with a populated demo-pro July (over-budget lead + real Pets drop mover + €915 cashflow, all numbers faithful). No schema change, no migration. Spec: `docs/features/monthly-review-narrative-spec.md`.

- **Fix: Trusted Client IP for Rate-Limit Buckets** — Security fix (2026-07-03 review, finding #1, Medium): `getClientIp` (`src/lib/rate-limit.ts`) read the **first** `x-forwarded-for` entry — a client-controlled prefix, since proxies *append* the real client hop to the right — so an attacker could spoof a fresh IP per request and mint a brand-new rate-limit bucket every time, defeating every IP-keyed limiter (login `${ip}:${email}` → unlimited online brute force against one account with only bcrypt cost as a brake; forgot/reset-password → reset-email floods + token guessing; register → account spam). Fix: prefer the platform-set **`x-real-ip`** (Vercel's edge sets it; not client-forgeable), else take the **LAST** `x-forwarded-for` entry (the platform-appended hop), else the unchanged `"unknown"` fallback (local dev, fail-open anyway). The doc comment states the load-bearing invariant: this trusts **exactly one proxy hop** (Vercel's edge) — fronting the app with an additional proxy (WAF, second CDN) changes the trusted index and must revisit this, or the new proxy's IP becomes one shared bucket for all users. Hop policy lives entirely in `getClientIp`: **no call-site changes** (all five callers — `auth.ts` authorize + the four auth API routes — unchanged), no new constant (structural, not tunable), no schema change, no migration. Backward compatible: legitimate clients behind Vercel resolve to the same IP as before; only spoofed prefixes stop working. `getClientIp` test suite grew 3 → 6 cases (`test/lib/rate-limit.test.ts`): `x-real-ip` wins over a spoofed XFF, `x-real-ip` alone, single-entry XFF verbatim, multi-entry XFF returns the last (trusted) hop — the exact case the old code got wrong, asserting `"203.0.113.7"` where pre-fix code returned the spoofable `"9.9.9.9"` — whitespace trimming, and the `"unknown"` fallback. **826 total tests pass** (was 823), build clean. Out of scope (separate specs): JWT session revocation (`jwt-session-revocation-spec.md`), security response headers (`security-headers-spec.md`), hard-requiring Upstash in prod. Spec: `docs/fixes/trusted-client-ip-spec.md`.

- **Fix: Reject Reset-Namespace Tokens in Verify-Email Consume Path** — Security fix (2026-07-03 review, finding #4, Low): verification and password-reset tokens share the `VerificationToken` table, split only by the `identifier` column (`reset:<email>` vs bare `<email>`). `consumePasswordResetToken` already refused cross-namespace rows, but `consumeVerificationToken` (`src/lib/auth/verification.ts`) consumed any hash-matched row **without checking the namespace** — so a password-reset raw token fed to `GET /api/auth/verify-email?token=...` would (1) **delete the row, burning the user's legitimate unexpired reset link** (denial of the reset flow by anyone who could route the raw token there), and (2) return `identifier = "reset:user@example.com"`, which the route passed to `prisma.user.update({ where: { email } })` → no match → throw → **500**. Fix mirrors the reset side's guard exactly: `RESET_PREFIX` (`"reset:"`) exported from `src/lib/auth/password-reset.ts` (single source of truth, no re-typed literal; no import cycle — `password-reset.ts` doesn't import `verification.ts`), and `consumeVerificationToken` folds `record.identifier.startsWith(RESET_PREFIX)` into its early `null` return **before** the `delete` — key ordering point: unlike the reset side (which deletes-then-validates because a genuine reset token *should* be single-use-consumed there), the misrouted reset row is left **intact**, and the route's existing `null` handling redirects to `/verify-email?error=invalid` instead of 500ing. Two-file change, no route change, no schema change, no migration; legitimate bare-email verification tokens behave identically. Test: added the mirror case to `test/lib/auth/verification.test.ts` (reset-namespace row → `null` + `delete` NOT called), with a comment tying it to the pre-existing inverse test on the reset side ("rejects a bare-email verification token without deleting it") — the two consume paths are now provably mirror images; unknown/expired/happy-path regression guards already existed. **827 total tests pass** (was 826), build clean. Out of scope (separate specs): JWT session revocation (`jwt-session-revocation-spec.md`), security response headers (`security-headers-spec.md`). Spec: `docs/fixes/verify-email-reject-reset-token-spec.md`.

- **Smart Budget Suggestions (Pro, AI) (Post-MVP §6)** — The last of the four committed AI capabilities before the Pro Value Review checkpoint. A Pro-only **"Suggest budgets"** panel above the `/budgets` list/empty state proposes per-category ceilings from the user's own history, each a **pre-filled, editable, one-tap-accept row** through the existing `createBudget` — **the AI never writes**. **Load-bearing rule (D1): every amount is deterministic** — a pure `buildBudgetSuggestionFacts` (`src/lib/budget-suggest.ts`) computes each ceiling (median of months-with-spend over the 3 complete months before the viewed period, rounded **up** to an adaptive step via `roundUpToStep` — `BUDGET_SUGGEST_ROUND_STEPS` €5 <€100 / €10 ≤€500 / €25 above — clamped to `BUDGET_AMOUNT_MAX`); the model only phrases a one-line rationale, and `validateSuggestionNotes` **drops any note whose numbers aren't in that suggestion's facts**, so a hallucinated euro figure can never become a ceiling. Eligibility floors (D8): ≥ `BUDGET_SUGGEST_MIN_MONTHS_WITH_SPEND` (2) lookback months with spend **and** median ≥ `BUDGET_SUGGEST_MIN_MEDIAN` (€10) — one-off/noise categories never surface; categories with an **active** budget in the target period are excluded (archived slots stay suggestible — revived by `createBudget`'s upsert, matching `getBudgetFormData`), and the uncategorized (`null`) key is dropped in the fetcher (a budget requires a category). Deterministic **spike** flag (`max > 1.5 × median`) and **variability** chip (D11 — `(max − min) / median ≥ 0.5` → "variable" vs "consistent") are **informational only, never change the math**. Output is fully deterministic (D7): ranked by **average spend desc**, case-insensitive name tie-break, capped at `BUDGET_SUGGEST_MAX = 8` — identical history → identical suggestions. **Window-agnostic fact builder (D12):** `lookbackMonths` is derived from the input array; a `weights` seam is reserved (uniform, not implemented in v1); `BUDGET_SUGGEST_LOOKBACK_MONTHS = 3` is consumed only at the `getBudgetSuggestInputs` call site. Budgets are **not account-scoped** — spend aggregates across all accounts via the split-aware `getCategorySpend` (one call per lookback month, same shape as `resolveRolloverCarry`), so a suggested ceiling and the bar that later tracks it agree by construction. Thin `suggestBudgets` action over `runAiFeature` (`feature: "budget_suggest"`, `burstLimit: "aiSuggest"` — own bucket under the shared 20/h policy, **no new `RATE_LIMITS` entry**); `hasSuggestSignal` false → `AiNoMatchError` → `reason: "no_match"` → quiet "Not enough spending history yet to suggest budgets." **Fail-open twice over:** an orchestrator failure degrades to the manual budget flow; a mere **phrasing** failure degrades to deterministic fallback copy — *"Median of your last 3 months of X spending."* — not to nothing (**D5, softer than §5's fail-open by design**), with `aiNotes: false` switching the caption ("Computed from your data." vs "AI-phrased — amounts computed from your data.") and `ai_phrasing_degraded { feature, prompt_version, reason }` keeping it measurable (the orchestrator's `ai_result` stays "ok" in that path, so this event is the real AI-health signal). **§5.4 refactor (the one existing-code touch):** extracted the shared numeric-guard core — `lineNumbersAllowed`, the `AllowedNumbers { money, pct }` shape, the €/separator token normalizer, and `NUMERIC_GUARD_MONEY_EPSILON`/`NUMERIC_GUARD_PCT_EPSILON` — from `src/lib/ai/review.ts` into `src/lib/ai/numeric-guard.ts`; `review.ts` now imports them (behavior-preserving — the pre-existing `test/lib/ai/review.test.ts` passes **untouched**, the extraction proof). **Reuses the §3 AI foundation unchanged** (`runAiFeature`/`aiJsonRespond`/`getAiProfile`/`track`/`AiParseError`/`AiNoMatchError`/`--color-ai`) — prompt + parse + facts step + panel only; **no new client, orchestrator, Pro read, rate policy, telemetry sink, color, schema, or migration** (third proof of §3's D6 after §4 and §5). **UI:** Pro-only AI-accented panel (`border-ai/25` + Sparkles chip, mirroring the §5 card) — collapsed "Suggest budgets" band → result rows (`resolveIcon` + `<CategoryIcon size="sm">` + name + variability chip + rationale + editable `€` amount input pre-filled with `suggestedAmount` + per-row **Add**), **"Add all"** (sequential `createBudget` calls — sole-writer rule over round-trips, client-side success tally since `createBudget` returns no `data`), explicit **Dismiss** (fires `ai_budget_suggest_dismissed { prompt_version, suggested_count, accepted_count }`, collapses); period stepping remounts the panel via the page's `Suspense key`, clearing stale results, with a `suggestRunRef` monotonic guard for in-flight results; accept fires `ai_budget_suggest_accepted { prompt_version, edited }` (`edited = amount !== suggestedAmount`); `reason`-driven fail-open copy (`no_match` / `rate_limited` / else). Telemetry is flags/counts only — no amounts or category names (the shim's no-PII contract). **Deviations from spec (repo wins):** `createBudget` returns `MutationResult` (no `data`) so "Add all" tallies successes client-side; the `run.ts` `reasonForThrow` mapper is private, so the action carries its own small `reasonLabelForThrow` for the `ai_phrasing_degraded` reason; the numeric-guard interface is the repo's `AllowedNumbers { money, pct }`, not the spec's `{ amounts, percents }`; `toBudgetSuggestions` `flatMap`-drops any row whose category isn't in the lookup (the builder only emits known ids, so inventing display attributes would be worse than dropping) rather than substituting a default icon/color. New files: `src/actions/ai/{suggest-budgets,track-budget-outcome}.ts`, `src/lib/budget-suggest.ts`, `src/lib/db/budget-suggest.ts`, `src/lib/ai/prompts/budget-suggest.ts`, `src/lib/ai/budget-suggest.ts`, `src/lib/ai/numeric-guard.ts`, `src/components/budgets/budget-suggestions.tsx`. Constants: 7 `BUDGET_SUGGEST_*` knobs (system-constants) + `BUDGET_SUGGEST_PROMPT_VERSION = 1` (prompt) + `suggestBudgetsSchema` (validations/ai). **53 new tests** (`test/lib/budget-suggest.test.ts`, `test/lib/ai/budget-suggest.test.ts`, `test/lib/ai/numeric-guard.test.ts`, `test/actions/ai/suggest-budgets.test.ts`) — **880 total pass**, build + lint clean (0 errors; pre-existing advisory warnings only). Verified the deterministic core live (read-only) against demo-pro's Apr–Jun seed history on the `development` Neon branch: floors drop one-off Doner/Pets/Misc, median resists May's €786 Dining spike, active-July budgets excluded → 4 sensible suggestions (Housing €1300, Utilities €140, Health €65, Subscriptions €30). No schema change, no migration. Docs: POST-MVP-ROADMAP §6 + Delivery slot 11 shipped (next: Pro Value Review checkpoint); project-overview Budgets note; `/help` Budgets line. Also this slice: re-sorted the History section of `current-feature.md` into true chronological order (test-count-derived). Spec: `docs/features/smart-budget-suggestions-spec.md`.

- **In-App Notifications (Post-MVP §9)** — Extended the dashboard insights strip into an app-wide **notification bell + popover panel** in the topbar (present on every `AppShell` page), answering "is there anything that needs me?" off `/dashboard`. Strictly **rung 1 (derive-first)** of the escalation ladder: **no `Notification` table, no read/unread, no dismissal state (server or client-side), no email/push** — resolving an item on its linked page is the only "dismiss". The panel shows **per-entity, derived, link-out items** for four kinds — `budget-over` (danger), `budget-risk` (warning), `draft` (info), `goal-overdue` (warning) — realizing the over/at-risk **severity split** the insights spec deferred, built from the **same single-sourced rules** as the strip. **The load-bearing extraction:** pulled the at-risk threshold rule out of `countAtRiskBudgets` into one carry-aware `budgetRiskLevel(row) → "over" | "at-risk" | null` (`src/lib/insights.ts`) so the strip's aggregate count and the panel's per-row severity can never disagree — `countAtRiskBudgets` now delegates (`!== null`), and the **existing insights suite passes untouched** (extraction proof). Pure builder `buildNotificationItems` (`src/lib/notifications.ts`): classify via `budgetRiskLevel` → per-kind module-private label helpers (the i18n seam) → fixed ordering (budget-over worst-first by uncapped magnitude → budget-risk percent desc → draft date asc → goal-overdue name asc) → per-kind cap `NOTIFICATION_GROUP_MAX = 5` emitting a synthetic `"+N more"` overflow row flagged `isOverflowRow: true` (`:more` id suffix incidental — consumers branch on the flag, never id shape) → pre-cap `counts` per kind + `totalCount` with the **tested invariant `totalCount === sum(counts)`** (overflow rows never counted). No `Decimal` crosses the builder boundary (narrow structural inputs, no DB row types). **Channel-agnostic derivation:** `deriveNotifications(userId, now?)` (`src/lib/db/notifications.ts`, `server-only`, injectable `now` for tests + future cron callers) composes the existing dashboard fetchers (`getBudgetsData` / `getPendingDrafts` / `getGoalsSummary`) and feeds the builder — **no new queries**; the upper ladder rungs (rung-2 persistence cron, rung-3 email digest) would call this same function so every channel derives identically. **Lazy Server-Action fetch, not prop threading:** thin read-only `getNotifications` (`src/actions/notifications.ts`, auth + telemetry + error shape, no `revalidatePath`, no rate limit — matches `loadMoreTransactions`) called by the bell on mount (per-navigation freshness, since `AppShell` remounts) + on panel open, so **no wiring tax across the 10 `AppShell` pages** and it **never blocks paint**; a monotonic run-token guard discards stale responses, and the panel fails closed (badge-less bell + error line, never breaks the topbar). **UI** (`src/components/notifications/notification-bell.tsx`, client): Lucide `Bell` + count badge (suppressed at 0, capped `9+` via `NOTIFICATION_BADGE_MAX`), popover with a `TONE_CLASS`/`DOT_CLASS` semantic-color lookup (`danger`/`warning`/`info`, no new tokens), calm `"All caught up."` empty line, whole-row `next/link` firing click telemetry + closing on click; mounted in `topbar.tsx` between the spacer and the account pill. **Telemetry** via the no-op `track()` shim (`notifications_derived` with total + per-kind counts, `notification_panel_opened`, `notification_item_clicked` — counts/enums only, no PII) — the rung-2 evidence the roadmap gates persistence on. **Not Pro-gated, no account scoping (ignores `?account=`), no draft generation, no schema change, no migration, no new route.** **Fix during QA (mobile):** the bell sits mid-topbar (account pill to its right), so the initial `absolute right-0 w-80` popover spilled off the left edge of a phone; the panel is now a near-full-width sheet pinned under the topbar on mobile (`fixed left-2 right-2 top-14 max-h-[70vh]`) reverting to the anchored `w-80` popover at `sm+` (bottom-sheet variant stays deferred per spec). Constants `NOTIFICATION_GROUP_MAX` / `NOTIFICATION_BADGE_MAX` (UI, `constants.ts`); types in `src/types/notifications.ts` (`NotificationItem`/`Kind`/`Tone`/`NotificationsPayload`, with the normative `id`-is-not-a-persistence-key contract). Docs: POST-MVP-ROADMAP §9 shipped banner + Delivery slot 12; project-overview topbar `"No bell icon"` → shipped; `/help` Common-questions line. **32 new tests** (`test/lib/notifications.test.ts`, `test/lib/db/notifications.test.ts`, `test/actions/notifications.test.ts` + `budgetRiskLevel` cases appended to `test/lib/insights.test.ts`) — **912 total pass**, build + lint clean. Spec: `docs/features/in-app-notifications-spec.md`.

- **Subscription Detection (heuristic v1) (Post-MVP §10)** — A **"Suggested templates" panel on `/recurring`** that spots regular spend the user re-types by hand (Netflix, rent, gym, salary) and **suggests** "make this a recurring template?" — the first feature to *read* the `merchant` field structurally (the payoff for stamping `merchant` on confirmed drafts + preserving it on import). **Pure deterministic heuristic, zero AI coupling** (no `runAiFeature`, no prompt, no `--color-ai`, **no Pro gate, no rate limit, no account scoping**) — the roadmap's committed v1 scope; v2 AI merchant-normalization stays deferred + evidence-gated. **Never auto-creates** — a suggestion pre-fills the existing `TemplateFormDrawer` (unsaved) and `createTemplate` stays the sole writer, keeping the confirm moment twice over (accept the template → confirm each draft). **Engine** (`src/lib/recurring-suggest.ts`, pure/deterministic, injectable `now` + `config`): group candidate rows by `(normalizeLabelKey(merchant), type)` → strict **six-rule gate** (≥`SUBSCRIPTION_MIN_OCCURRENCES`=3 occurrences; a WEEKLY 6–8d / MONTHLY 26–35d cadence band the **median gap selects and every interval fits** — no outlier allowance, so a same-day dup (gap 0) or interleaved one-off correctly kills it; amounts regular within ±`SUBSCRIPTION_AMOUNT_TOLERANCE`=0.15 of the median; series **alive** ≤ band `maxDays × SUBSCRIPTION_STALE_FACTOR`=1.5; median ≥ `SUBSCRIPTION_MIN_AMOUNT`=€5) → suppression (existing template name normalized, active *or* paused; muted key) → deterministic derived values (name = most-recent raw casing; amount = `round2(median)`; account = most-recent; category = **mode with most-recent tie-break**; `nextOccurrence` = `advanceNextOccurrence` stepped **until ≥ today** so no overdue-draft backlog) → rank median-amount desc, name-asc tie-break, cap at `SUBSCRIPTION_SUGGEST_MAX`=5. Returns `{ suggestions, detectedCount }` — **pre-cap count preserved** (the notifications payload's rule) so telemetry distinguishes "cap throttling signal" from "heuristic finds nothing"; tested invariant `suggestions.length === min(detectedCount, suggestMax)`. **Precision over recall** is the governing bias (a missed sub costs nothing — manual create; a false nudge is nagging). **Derive on page load** — `getRecurringSuggestions(userId)` (`src/lib/db/recurring-suggest.ts`, `server-only`, injectable `now`) joins the existing `/recurring` `Promise.all`: one bounded 12-month candidate query (six §3 predicates, select-only, date-asc, `Math.abs(Number(amount))` so no `Decimal` crosses the pure boundary) + templates + mutes in parallel; cron-free like `generatePendingDrafts`; emits the `recurring_suggested` impression event (detected/surfaced/weekly/monthly counts). **One additive schema model** — `RecurringSuggestionMute` (`@@unique([userId, merchantKey])`, migration `add_recurring_suggestion_mutes`, applied to the `development` Neon branch; no existing table touched) — persistence justified where §9 notifications refused it (a false positive has **no resolution path** — the historical rows are permanent, correct ledger entries — so "not a subscription" is user intent that must persist, like `Budget.rollover`). **`muteRecurringSuggestion`** action (extends `src/actions/recurring.ts`; `muteSuggestionSchema` in `validations/recurring.ts`): auth-guarded, server **re-normalizes** the key (never trusts the client), idempotent `upsert`, shared by **both** outcomes — Dismiss fires it directly, and **accept also mutes** (fire-and-forget on drawer save-success) so a merchant stays suppressed even after a rename in the drawer; accepted/dismissed distinction lives only in telemetry. **Normalizer extraction (behavior-preserving):** the import resolver's trim→collapse→NFC→lower pipeline moved to shared `normalizeLabelKey` (`src/lib/text.ts`); `normalizeCatKey` now delegates — the existing `test/lib/import/*` suites pass **unmodified** (the extraction proof). **UI:** `SuggestionsPanel` (plain `bg-surface`/`border-line` surface, **no** `--color-ai` — deterministic math, not AI; renders nothing when empty) above `TemplatesList`/empty state, each row = merchant + cadence chip + `formatCurrencyCents` median + evidence line ("4 charges · last Jun 15") + **Create template** (opens the drawer pre-filled) and a **Dismiss X** (neutral at rest → **destructive-red on hover**, matching the app's icon-button convention); `TemplateFormDrawer` gained a `prefill`/`onCreated` pair (create branch seeds every field from the suggestion; `onCreated` awaited before close so the accept-mute lands before the page re-derives); `RecurringView` carries the selected suggestion in its `DrawerState` and owns `handleCreated`; page `Suspense` key extended with `suggestions.length`. **All thresholds are `SUBSCRIPTION_*` constants** (system-constants), also injectable into the engine as a `config` — telemetry-driven recalibration is a one-file edit, zero algorithm change. Reuses `advanceNextOccurrence` (`recurring.ts`) + `round2` (`money.ts`). Telemetry rides the no-op `track()` shim (`recurring_suggested` / `recurring_suggestion_accepted` / `recurring_suggestion_dismissed`, counts/enums only). WEEKLY + MONTHLY only (DAILY = habit noise, YEARLY needs 2+ years — deferred). Docs: POST-MVP-ROADMAP §10 + Delivery row 13 shipped; project-overview Out-of-Scope graduation + Recurring feature note; `/help` Recurring line. **Finding:** the demo seed's repeated merchants (Employer/Landlord same-day duplicates → gap 0; Whole Foods/Transit amount-irregular) correctly surface **nothing** — precision guard working; provided the user a SQL batch of clean series to exercise it live. **49 new/updated tests** (`test/lib/recurring-suggest.test.ts`, `test/lib/text.test.ts`, `test/lib/db/recurring-suggest.test.ts` + `muteRecurringSuggestion` block in `test/actions/recurring.test.ts`) — **961 total pass**, build + lint clean (0 errors; pre-existing advisory warnings only). No AI, no schema-beyond-mute, no new client/orchestrator/rate policy. Spec: `docs/features/subscription-detection-spec.md`.

- **Cash-Flow Forecast (Post-MVP §18)** — A read-only **`ForecastPanel` card on `/dashboard`** (right column, below `GoalsWidget`) projecting the balance **30 days forward** from **active recurring templates + pending drafts** — *"you'll dip to €240 around the 28th, before salary."* Forward-looking **state-adjacent awareness**, the triad-completer next to the hero (*where am I*) and sparkline (*where have I been*): *where am I heading given what I've already scheduled*. **Zero AI, zero writes, zero schema, zero migration, no Pro gate** — a pure read-side projection over data the app already owns. **Engine** (`src/lib/forecast.ts`, `buildCashflowForecast`, pure/deterministic, injectable `now` + `horizonDays`): expand signed events inside `[today, today+FORECAST_HORIZON_DAYS]` (both inclusive, `today = startOfUtcDay(now)`) — each pending draft → one event at `max(startOfUtcDay(suggestedDate), today)`; each active template → one event per occurrence stepped via the existing `advanceNextOccurrence` — then fold into a running **end-of-day** balance series (`points[d] = { date, balance: round2(...) }`), returning `{ points (length horizon+1), low (min, earliest day wins tie), end, eventCount }`. Reuses `advanceNextOccurrence` + `startOfUtcDay` + `round2` — **no new date/money math**; module-private helpers `expandTemplateEvents` + `signedAmount`. **The load-bearing skip rule (§4.1.2):** a template whose `hasPendingDraft` is true starts stepping from `advanceNextOccurrence(nextOccurrence)` (not `nextOccurrence`) — because `nextOccurrence` isn't advanced until `confirmDraft`/`dismissDraft` runs, so projecting it would **double-count** the occurrence the draft already contributes; asserted via an **equivalence test** (a draft-minted template + its draft produces the *identical* series to the not-yet-minted no-draft template). Overdue occurrences **clamp to day 0** (stepping continues from the *unclamped* date so the series' rhythm holds); a deep backlog piles on day 0, honestly (that money is due now if confirmed). DAILY + YEARLY **included** (explicitly-scheduled items, unlike §10's detection bands). The loop's true terminator is `cursor <= horizonEnd`; the `MAX_ITERATIONS`/non-advancement break are pure infinite-loop backstops a correct `advanceNextOccurrence` never trips (**fixed a first-draft bug** where a `horizonDays+2` cap would have truncated a deeply-overdue template's legitimate day-0 backlog). **Anchor = `summary.totalBalance`** (the hero number): the fold happens **in-process on the page** (D6) from the already-fetched summary, so the projection can **never drift** from the number rendered beside it; the fetcher `getScheduledItems` (`src/lib/db/forecast.ts`, `server-only`) reads scheduled items only — active templates (`isActive`, non-archived account, `hasPendingDraft` via a `take: 1` PENDING sub-read) + pending drafts (PENDING, non-archived account, type from the parent template) — mapping every amount to `Math.abs(Number(...))` so **no `Decimal` crosses the pure boundary** (sign derived from `type` in the engine, as `confirmDraft` does at write time). **UI** (`forecast-panel.tsx`, server component — no interactivity): header `Projected balance` + meta `Next 30 days · assumes scheduled items are confirmed` (horizon derived from `points.length`, never hardcoded); a dependency-free **module-private SVG** (not a `Sparkline` reuse — that's `aria-hidden`, solid, min/max-normalized) with a **dashed neutral line** + faint area (the one cue this is a projection, not history), a `points[0]` "today" dot, and — only when the path crosses below zero — a zero baseline with the below-zero line/area segments in `--color-danger` (via two `clipPath` bands split at `zeroY`), never success-green (which the hero owns and would over-promise a guess); `role="img"` + a data-summarizing `aria-label`, decorative shapes `aria-hidden`; a real-DOM **facts row** (`Lowest: €X · around <date>`, danger-colored when < 0; `In {30} days: €X` + signed delta) as the load-bearing content the chart illustrates. **Hidden entirely at `eventCount === 0`** (D7 — a flat line is decoration; the demo-nonpro user with no templates sees no card). Page wiring: `getScheduledItems` joins the dashboard `Promise.all` (8→9), the fold runs after, `<ForecastPanel>` renders below `GoalsWidget` inside the `accounts.length > 0` branch. **Freshness for free** — `/dashboard` is `force-dynamic` and every recurring mutation already `revalidatePath("/dashboard")`s (confirm a draft / pause a template → projection re-anchors, zero new wiring). One constant `FORECAST_HORIZON_DAYS = 30` (the `horizonDays` param leaves the 60/90 seam open). Decisions: D1 Dashboard right column below Goals; D2 fixed 30d; D3 drafts AND templates w/ skip rule (paused excluded); D4 free; D5 all active accounts, **ignores `?account=`** (extends the all-accounts hero, so scoping one but not the other would contradict it); D6 in-process fold; D7 hide at zero; D8 dashed neutral / danger-below-zero. **Dev tooling:** `scripts/add-demo-salary.ts` (idempotent) gives demo-pro an INCOME "Salary" template (+€2400 MONTHLY, next 2026-07-25) so the card shows the classic **dip-then-recover** shape (subscriptions trickle down, salary jumps it back up); verified live on the `development` Neon branch (demo-pro's 6 subscription templates + the new salary land in-window). Docs: POST-MVP-ROADMAP §18 shipped banner + Delivery row 14 + open question #10 (forecast half) resolved; project-overview Dashboard note (the "Dashboard is for state" principle **upheld** — forward-looking state, not analytics); `/help` Recurring "Projected balance on the dashboard" line. **24 new tests** (`test/lib/forecast.test.ts` — empty/signing/clamp/skip-rule equivalence/overdue-backlog/horizon-boundary/DAILY+YEARLY/month-end-clamp/low-tie/round2/below-zero/determinism; `test/lib/db/forecast.test.ts` — both query where-shapes, `Decimal`→magnitude mapping, `hasPendingDraft` derivation) — **985 total pass**, build + lint clean (0 errors; pre-existing advisory warnings only). No schema change, no migration. Spec: `docs/features/cash-flow-forecast-spec.md`.

- **Security Response Headers — Stage A (2026-07-03 review, finding #3)** — Emit app-wide browser hardening on **every** response via a `headers()` async fn in `next.config.ts` (`source: "/:path*"`). **Baseline headers enforced immediately:** `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=(), microphone=(), geolocation=()`, `X-DNS-Prefetch-Control: off`. **CSP shipped as `Content-Security-Policy-Report-Only` first (Stage A)** so the policy can be tuned against real traffic before it blocks — backed by a new **log-only `POST /api/csp-report`** sink (`src/app/api/csp-report/route.ts`: parse envelope → log one compact line [`blockedUri`/`violatedDirective`/`documentUri`, both kebab-case `application/csp-report` and camelCase `reports+json` shapes] → **always 204**; body size-capped via `CSP_REPORT_MAX_BYTES = 16 KB`, malformed/empty-tolerant, no auth/reads/writes — one of the render-cycle API exceptions, and ESLint's no-Prisma-in-`api` boundary holds since it touches no DB). The one non-trivial CSP allowance is **`form-action 'self' https://accounts.google.com`** for the Google OAuth form-POST redirect (Chrome applies `form-action` to post-submission redirects); **Stripe needs none** — verified no client-side Stripe.js and no iframes anywhere, so `frame-src 'none'` is honest; Resend is server-side only. `'unsafe-inline'` in `script-src` is defense-in-depth debt (no `eval`/`new Function`/`dangerouslySetInnerHTML` in `src/`, grep-confirmed) — nonce-based CSP is the deferred stronger end state. **Headers live in `next.config.ts`, not `<meta>`**, because `frame-ancestors`/`form-action` can only be set as response headers. **Verified end-to-end in Chromium (`next start`, driven via Playwright + demo-pro):** all five baseline headers present on page **and** API responses (curl); CSP present as Report-Only with the exact candidate policy; the report pipe works (injected `connect-src` violation reached both console and the sink); sink returns 204 on well-formed/malformed/empty bodies. **Load-bearing check passed** — clicking "Continue with Google" redirected to `accounts.google.com` with **zero `form-action` violations** (the allowance is correct). Authenticated sweep (dashboard, reports SVG charts, budgets, transactions, goals, settings, transaction drawer) produced **zero** CSP reports; a stray `eval`/RSC-prefetch report appeared **only** during transitional `chrome-error` navigations caused by a local `AUTH_URL=:3000`↔`:3100` port mismatch (not a correct-deploy condition) and did not reproduce during clean browsing — so **no policy change was made** and `'unsafe-eval'` was deliberately **not** added. **Stage B (flip the header name to enforcing `Content-Security-Policy`, at which point `frame-ancestors 'none'` supersedes `X-Frame-Options`) is intentionally deferred**, gated on a clean real-Chrome window (an actual Google login incl. the JS-disabled form-POST path + the Stripe Checkout/Portal round-trip) and a preview-deploy soak watching `/api/csp-report`. HSTS stays Vercel-edge-provided. Adds `CSP_REPORT_MAX_BYTES` (system-constants). **No new tests** (headers/config are out of the Vitest `src/lib`/`src/actions` scope) — **985 total still pass**, `npm run build` + lint clean (0 errors; pre-existing advisory warnings only). No schema change, no migration, no new dependency. Spec: `docs/fixes/security-headers-spec.md`.

- **Codex Integration (tooling, not app code)** — Run OpenAI **Codex CLI** alongside Claude Code in this repo, **fully project-scoped**: everything Codex needs lives in-tree and gitignored, nothing lands in `~/.codex`, nothing is `npm i -g`'d. **Two containment levers:** `CODEX_HOME` pinned to `./.codex` by launchers `codex.ps1` (PowerShell) + `codex.sh` (bash/Git Bash), and `@openai/codex` installed as an **exactly-pinned** (`0.142.5`, no caret) local devDependency. **Shared instructions (Step 2):** a Spendly preamble injected into `AGENTS.md` between `<!-- BEGIN/END:spendly-codex -->` markers **outside** the tool-managed `nextjs-agent-rules` block (so regeneration can't clobber it), with the **Neon guardrails inlined** (project `lucky-hat-53091250`, `development`/`br-hidden-bonus-aqksw1pa` only, never production `br-falling-haze-aqyvxnbq`, `prisma migrate dev` not `db push`, no agent attribution) — because Codex ingests `AGENTS.md` literally and does **not** expand Claude's `@docs/...` imports, so a bare pointer could be skipped. **MCP (Step 3):** `.codex/config.toml` mirrors `.mcp.json` in Codex HTTP `[mcp_servers]` syntax — neon (HTTP, one-time `codex mcp login neon` browser OAuth, token persists in gitignored `.codex/`), context7 (HTTP key via `env_http_headers` reading `$CONTEXT7_API_KEY` — `env` block is stdio-only and rejected for HTTP), playwright (stdio). **Prompts (Step 4):** the three tracked `shared-prompts/feature-*.md` copied into `.codex/prompts/` as `/feature-load`→`/feature-start`→`/feature-complete`. **Anti-drift (Step 6b):** tracked secret-free `codex-setup/{agents-preamble.md,config.template.toml}` are the single source of truth; **idempotent** `scripts/sync-codex.ps1` re-applies the preamble (regex-replace between markers) + copies config-when-missing + syncs prompts (verified byte-identical on a second run); `scripts/verify-codex.ps1` is the automated Step-7 smoke test; `codex:sync` + `verify:codex` npm scripts. **Secrets DX:** launchers auto-load a gitignored **`.codex.env`** (`KEY=value`) so the context7 key is never typed by hand; `.codex.env.example` is the tracked placeholder. `.gitignore` adds `AGENTS.override.md` (§7 footgun — it *replaces* `AGENTS.md`, dropping both the Next.js block and the Neon rules; verify asserts none exists), `.agents`, `.codex.env`; new `.gitattributes` pins `*.sh` to **LF** so `codex.sh`'s shebang survives Windows `autocrlf=true` checkouts. **Deferred by decision:** Step 4b (port the workflow as a same-format Codex `SKILL.md`) and the Step 5 agent-role pilot. **Bugs found + fixed during build:** (1) verify ran the raw binary → `codex --version` leaked a `~/.codex`; repointed it through the launcher (which pins `CODEX_HOME`) so the version check doubles as a containment + launcher smoke test; (2) UTF-8-no-BOM `.ps1` files mis-decoded under Windows PowerShell 5.1 (an em-dash's stray byte closed a `Write-Host` string → exit 1 despite passes) — scripts are now ASCII-only. **Live-verified end-to-end** in a real Codex session: it recites the Neon branch rules verbatim (Step 2 works), `codex mcp list` shows all three servers `enabled`, neon authenticated via OAuth, context7 key auto-loaded from `.codex.env`, and Codex confirmed neon/context7/playwright **tools** are available (the empty `list_mcp_resources` was a resources-vs-tools distinction, not a fault). Not app code — **no `src/**` surface, so no Vitest** (the `.codex/config.toml` verify is the equivalent gate); `npm run verify:codex` all-pass, and the baseline `test:run` (985) + `build` stay green unchanged. `AGENTS.md`, `.mcp.json`, `.codex/`, `.codex.env` all remain gitignored (never committed). Spec: `docs/features/codex-integration-spec.md`.
