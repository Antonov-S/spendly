# Entity Types Reference → moved

**This file is not maintained.** The canonical, single source of truth for every Prisma model's
field-level reference now lives at:

➡️ [`../entity-types.md`](../entity-types.md) (i.e. `docs/entity-types.md`)

This stub used to hold a full copy that drifted out of sync with the top-level version (different
FinancialAccount delete strategy, stale currency rules, etc.). To prevent that class of bug, there
is now exactly one copy. Edit `docs/entity-types.md` only; do not restore content here.

Feature specs in this folder should link the reference docs as `../entity-types.md` and
`../entity-crud-architecture.md`.
