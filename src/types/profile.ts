/** Per-item-type record counts shown on the profile usage breakdown. */
export interface ProfileStats {
  financialAccounts: number;
  transactions: number;
  budgets: number;
  goals: number;
  recurringTemplates: number;
}
